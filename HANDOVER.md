# 인수인계 — 제주 가족여행 가이드

마지막 갱신 2026-08-28 · 커밋 `a224d1a`

---

## 1. 이게 뭔가

**2026.10.05~10.09 제주 4박 5일 가족여행 안내 웹앱.** 가족이 휴대폰으로 보는 것이 전부다.

- 배포 주소 **https://hanbrook3.github.io/jeju-trip/**
- 동행 — 아버지 · 어머니 · 아내 · 나. **부모님이 보신다**는 점이 UI 결정의 기준이다.
- 숙소 남원읍 서의로154번길 · 이동 진도↔제주 카페리
- **가족에게 카카오톡으로 공유된다.** 이게 여러 기술 결정의 원인이다(§5).

## 2. 파일

```
index.html    1,445KB   앱 전체. HTML·CSS·JS·Leaflet·사진이 한 파일에 다 들어 있다
sw.js             3KB   서비스워커 (오프라인 + 새 버전 알림)
manifest.webmanifest    PWA
icon-192.png / icon-512.png
.github/workflows/pages.yml   배포
tools/serve.js              로컬 확인용 서버
tools/regression-check.js   회귀 점검 — 손댈 때마다 돌린다
tools/mapdata/              개략도 밑그림 자료와 파이프라인 (자체 README 있음)
```

**한 파일 구조는 의도한 것이다.** 링크 하나만 던지면 열리고, 저장해서 오프라인으로도
쓸 수 있어야 하기 때문이다. 쪼개자는 제안은 이 전제를 먼저 확인하고 할 것.

## 3. `index.html` 어디에 뭐가 있나

줄 번호는 고칠 때마다 밀리므로 **`grep -n "const NAME="` 로 찾는다.**

| 이름 | 내용 | 건수 |
|---|---|---|
| `PLACES` `POI` | 지도 위 지명 라벨 / 주변 명소 점 | 10 / 71 |
| `COAST` `ISLES` `CONT` `RD` | 개략도 밑그림 (§6) | 881점 / 12섬 / 5단 / 57선 |
| `PATHS` | 하루치 주행 경로 (네트워크 없이도 같은 선을 그린다) | 5일 |
| `VEC` `VW` | 개략도 색 / 배율별 선 굵기 | — |
| `TILE_SRC` | 타일 서버 목록 (§5) | 2 |
| `AREAS` | 지적 경계 폴리곤 (브이월드 연속지적도) | 11 |
| `D` | **일정 본문.** base64 사진이 여기 들어 있다 | 5일 · 정차지 52 (좌표 있는 것 30) |
| `CAND` | 준비물 탭에 붙는 일정 연계 식당 | 9 |
| `SPOT` | 여행지 탭 (운영시간·휴무·유래·건축가·관광포인트·주소) | 48 |
| `FOOD` | 맛집 탭 (식당 33 · 카페 18 · 주소·전화) | 51 |

주요 함수 — **`drawMapFor` · `redrawMap`(세 탭의 지도를 그리는 단 하나의 길, §12)** ·
`MAPPTS`(탭마다 어떤 점에 맞출지) · `drawDayPins`(일정 탭 번호 핀) ·
`buildVector` `vecZoom`(개략도) · `mountTiles` `retryTiles`(타일) ·
`drawPoi` `declutter`(점 정리) · `focusStop` `focusPoi` `fitMap` `fitCurrent`(지도 이동) ·
`scrollUnderMap`(지도 점→카드) · `openCard`(지도 점을 누르면 그 카드를 연다) ·
`markSel` `markSelAt`(고른 점 강조) · `toggleMapFull`(전체보기) · `staticMap`(Leaflet 조차 없을 때).

배율 — `ZOOM_THEME` `ZOOM_TY` `spotZoom()`(항목마다 다르다, §11) ·
`ONE_ZOOM=14`(목록에 하나만) · `JEJU`(섬 전체 범위).

---

## 4. ⚠️ 먼저 읽을 것 — 모르면 반드시 시간을 버리는 것들

### 4.1 base64 사진 줄을 Read 하지 말 것

`index.html` 안에 사진 21장이 base64 로 박혀 있다. **한 줄이 최대 143KB.**
`Read` 로 그 줄을 열면 컨텍스트가 통째로 날아간다.

```bash
grep -n "data:image/" index.html    # 어느 줄인지 확인만
```

2026-08-28 기준 678줄(Leaflet 원본) 과 2344~2572줄 사이. 이 구간은
`sed -n` 으로도 열지 말고, 고칠 일이 있으면 **줄 단위가 아니라 앵커 문자열로** 접근한다.

### 4.2 브라우저 스크린샷이 안 된다

Browser pane 이 화면에 안 떠 있으면 `computer{action:"screenshot"}` 이 5초 만에
타임아웃난다(`tabs_select` 해도 안 될 때가 많다). 대신:

- **수치 확인** → `javascript_tool` 로 `getBoundingClientRect()` 등을 직접 재서 JSON 으로 받는다. 이게 주력이다.
- **개략도 모양 확인** → `node tools/mapdata/preview.js` 로 PNG 를 찍고 `Read` 로 연다.

`javascript_tool` 은 **30초 제한**이 있다. 대기시간을 합쳐 그걸 넘기면 실패하지만
페이지에서는 계속 돌아간다 — 결과는 뒤이어 짧은 호출로 확인하면 된다.
점검을 두세 덩어리로 쪼개는 편이 낫다.

### 4.3 파일 수정은 앵커 매칭 node 스크립트로

1.4MB 파일이라 `Edit` 도 되지만, 여러 곳을 한 번에 고칠 때는 이 패턴을 쓴다.
**하나라도 안 맞으면 아무것도 안 쓰고 죽는다**는 게 핵심이다(중간에 반쯤 고쳐진 상태가 안 생긴다).

```js
const R = [[찾을문자열, 바꿀문자열], ...];
let n = 0;
for (const [a, b] of R) {
  const c = s.split(a).length - 1;
  if (c !== 1) { console.error('실패(' + c + '건): ' + a.slice(0, 60)); process.exit(1); }
  s = s.split(a).join(b); n++;
}
fs.writeFileSync(P, s);
```

고친 뒤에는 반드시 문법 검사:

```bash
node -e "const h=require('fs').readFileSync('index.html','utf8');
(h.match(/<script>([\s\S]*?)<\/script>/g)||[]).forEach((b,i)=>{
 const c=b.replace(/^<script>/,'').replace(/<\/script>\$/,'');
 try{new Function(c);console.log(i+': OK');}catch(e){console.log(i+': '+e.message);process.exitCode=1;}});"
```

### 4.4 Bash heredoc 이 역슬래시를 먹는다

`cat > x.js <<'JS'` 안에 정규식(`\.` `\d` `\s`)을 쓰면 백슬래시가 사라진다.
정규식이 들어가는 스크립트는 **`Write` 도구로 파일을 만들거나 `Edit` 로 고친다.**
이걸로 서비스워커 정규식을 한 번 날려 먹었다.

### 4.5 Leaflet `flyTo` 는 못 믿는다

앞선 애니메이션이나 `invalidateSize()` 에 눌려 무시된다(A/B 테스트에서 9번 중 2번만 성공).
**지도 이동은 전부 `map.setView(..., {animate:false})`** 를 쓴다. 이미 그렇게 되어 있으니
되돌리지 말 것.

같은 이유로 **부드러운 스크롤(`behavior:'smooth'`)도 지도 이동에 취소된다.**
`scrollUnderMap()` 이 즉시 이동 + `requestAnimationFrame` + `setTimeout` 으로
세 번 맞추는 건 그래서다.

### 4.6 탭을 바꾼 직후 배율을 재면 거짓말이 나온다

탭 전환이 예약한 `fitMap` 이 나중에 실행되므로, 전환 직후 카드를 눌러 배율을 재면
그 항목의 배율(12~14) 이 아니라 9 로 나온다. **탭 전환 후 1.4초 이상** 기다리고 재야 한다.
회귀 점검이 실패하면 진짜 회귀인지 이 타이밍인지부터 가른다.

### 4.7 배포는 `site-deploy` 그룹의 커스텀 워크플로

GitHub Pages 의 기본 legacy 빌드가 쓰는 `pages` 동시성 그룹이 큐에 물려 멈춰 있었다.
`.github/workflows/pages.yml` 은 일부러 **`site-deploy` 라는 다른 그룹 이름**을 쓰고,
`actions/configure-pages` → `upload-pages-artifact` → `deploy-pages` 로 직접 올린다.
저장소 설정도 Pages 소스가 **GitHub Actions** 여야 한다(브랜치 빌드가 아니라).
이걸 "정리" 한다고 그룹 이름을 `pages` 로 되돌리면 배포가 다시 막힌다.

배포는 push 하면 자동이고 3~4분 걸린다. 확인:

```bash
gh run list --workflow=pages.yml --limit 1
curl -sS -o /tmp/live.html -w "%{http_code} %{size_download}\n" -H 'Cache-Control: no-cache' https://hanbrook3.github.io/jeju-trip/
```

### 4.8 서비스워커 두 가지 함정

- `e.waitUntil(update)` 를 **동기적으로** 불러야 한다. 응답을 돌려준 뒤에 부르면
  워커가 먼저 죽어 캐시 갱신이 취소된다.
- **지도 타일 호스트를 캐시 제외 규칙에 반드시 넣는다.** 타일 서버를 늘리면서
  이걸 같이 안 고쳐 OSM 타일이 캐시에 쌓인 적이 있다(OSM 이용정책 위반).
  규칙은 호스트명이 아니라 **도메인 단위**로 되어 있다 — `xdworld1/2/3` 같은 별칭까지 잡으려고.
- 캐시 내용을 강제로 비우려면 `CACHE` 이름을 올린다(현재 `jeju-trip-v3`).

---

## 5. 지도 타일 — 카카오톡 문제

**카카오톡 인앱 브라우저가 국토지리정보원(VWorld) 타일 서버를 막는다.** 가족은 카톡으로
링크를 열 테니, 이 상태가 예외가 아니라 기본에 가깝다.

대응은 3단이다. `TILE_SRC` 순서대로 시도하고 다 막히면 개략도로 내려간다.

| 순서 | 서버 | 특징 |
|---|---|---|
| 1 | `xdworld{s}.vworld.kr` (`s` = ``,`1`,`2`,`3`) | 한글 지도. 별칭 호스트로 동시 연결을 늘렸다. Referer 제한 없음 |
| 2 | `tile.openstreetmap.org` | 한글 라벨 완전. 고배율 정보량은 오히려 더 많다 |
| 3 | 개략도 (§6) | 서버가 필요 없다 |

실측으로 확인한 것:

- **OSM 은 Referer 가 없으면 조용히 죽는다.** HTTP 200 + `image/png` 로 6,987바이트짜리
  "Access blocked" 안내 이미지를 준다. `tileerror` 로 못 잡는다.
  그래서 `<meta name="referrer">` 를 `no-referrer` 에서 `strict-origin-when-cross-origin`
  으로 바꿨다. **다시 `no-referrer` 로 돌리면 OSM 폴백이 통째로 죽는다.**
- **VWorld 는 `Cache-Control` 이 없지만 ETag/Last-Modified 는 준다**(조건부 요청에 304).
  "캐시가 아예 없다"는 건 틀린 설명이다. 9타일 동시 요청 속도는 OSM 과 동률(0.38초).
- **에스리(Esri) 계열은 제주 z14 이상이 전부 빈 타일이다.** World Street / Topo /
  Light Gray 셋 다 같은 2,521바이트 `Map data not yet available` 이미지를 준다.
  바이트 수만 보면 정상처럼 보이므로 반드시 이미지를 열어 봐야 한다. 쓰지 말 것.
- **CARTO 는 키 없이 쓰면 워터마크가 도배된다.** HTTP·바이트 검사는 전부 통과한다.
- **OSM France 는 한글이 두부(□)로 깨진다.**

### 인앱 브라우저를 안 거치게 해 두었다 (2026-08-28)

**카카오톡에서 링크를 열어도 모바일 브라우저로 넘어가도록 되어 있다.** 그래서 인앱
브라우저의 타일 차단은 실제로는 잘 안 겪는다. 위 3단 폴백은 그대로 두되(다른 인앱
브라우저나 설정이 다른 기기가 있을 수 있다), **이것을 이유로 지도 서비스를 바꿀 근거로
삼지는 말 것.**

그래도 개략도가 나오면(왼쪽 위에 `개략도 · 눌러 다시 시도` 배지) 타일이 막힌 것이다.

---

## 6. 개략도

타일이 막혔을 때 나오는 밑그림. **손으로 찍은 도형이 아니라 실측 자료다.**

| 요소 | 자료 |
|---|---|
| 해안선 881점 | 통계청 2018 시도경계 |
| 부속 섬 12곳 | OpenStreetMap (우도·마라도·가파도·차귀도·비양도·형제섬 등) |
| 등고선 5단 | SRTM 90m 표고를 마칭스퀘어로 딴 것 (100·300·600·1000·1400m) |
| 간선도로 57선 | OpenStreetMap. 상·하행 중복 제거함 |

만드는 법과 다시 만드는 법은 **`tools/mapdata/README.md`** 에 있다.

### 설계 원칙 — 되돌리지 말 것

- **도로에 흰 테두리를 두르지 않는다.** 밑바탕이 밝아 테두리가 뭉개져 선이 부어 보인다.
  흰 테두리는 **오늘 갈 길(경로선) 하나만** 가져야 눈에 띈다.
- **밑그림은 물러나 있어야 한다.** 도로 대비는 육지 기준 1.6~2.1:1 로 일부러 낮다.
  WCAG 3:1 미달이지만 이건 장식이 아니라 의도다 — 그 위에 여행지 48 + 맛집 51 +
  일정 핀 + 지명 라벨이 얹힌다. 일정 핀은 육지 위 6.56:1, 가장 짙은 표고대 위에서도
  3.30:1 이라 어디서도 안 묻힌다.
- **글자는 색이 아니라 흰 테두리(text-shadow 4px)로 읽힌다.** 짙은 표고대 위에서
  글자색 대비가 2.8:1 까지 떨어지지만 halo 덕에 읽힌다. 글자색을 더 진하게 하면
  밑그림이 주인공처럼 보인다.
- **배율에 따라 덜어낸다.** 지선도로는 z≥10 에서만 나온다(`vecZoom`).

### 시도했다가 뺀 것

**해변 18곳** — 실제 폭이 200~600m 라 개략도가 쓰는 배율(z9~12)에서 1~12px,
대부분 3px 남짓이라 해안선 선에 덮여 안 보였다. 최대 배율에서도 실 조각 수준.
이름난 해변은 이미 `POI` 점으로 눌러볼 수 있어 중복이었다. 다시 넣지 말 것.

**오름 삼각형 20개** — 좌표가 임의값이었고 실제 368개의 5% 도 안 돼 분포를
나타내지도 못했다. 등고선이 그 역할을 한다.

---

## 7. 확인 절차

```bash
node tools/serve.js          # http://127.0.0.1:8765
```

브라우저를 375×667 로 맞추고 `tools/regression-check.js` 를 `javascript_tool` 에 붙여 넣는다.
**대기시간 합이 26초라 30초 제한에 걸리므로 섹션 단위로 서너 번에 나눠 붙인다**
(앞 결과는 `window.__rc` 에 모인다). 기대값은 그 파일 주석에 있다.
요약하면 — 배지불일치 0 · 핀합계 30 · 지적경계 11 · 경로 5 · 길찾기 0 ·
핀클릭 간격 8px · 배율 {미술관 14, 일출봉 13, 숲길 12, 맛집 14, 일정_일출봉 13} ·
탭통일 {패널 true, 안움직임 true, 선택표시 1} ·
점클릭 {일치 true, 간격 8} · 지도통합 {거친탭 "trip,spot,food", 한곳수정이_세탭에 true} ·
핀참조 {일정 5, 여행지 0} · 점갈래(일정 48/35/51 · 여행지 48/35/0 · 맛집 0/0/51) ·
섬전체 줌 9 · 범례 높이 29±1 한 줄 · 오류 0.

**1px 오차에 속지 말 것.** 지도 높이가 213.5px 이라 `Math.round` 가 213/214 사이에서
흔들린다. 범례 높이도 28.1px 이다. 판정에 쓸 값은 "한 줄 true" 쪽이다.

개략도 모양은 `node tools/mapdata/preview.js` → `preview.png` 를 `Read`.

폭별 확인이 필요하면 `resize_window` 로 320 / 360 / 375 / 430 을 돌려 본다.
**320px 에서만** 범례 문구가 "그 밖의 명소"→"명소" 로 줄어든다(`.lgmore`, `max-width:344px`).

---

## 8. 남은 일

### 확인이 필요한 것

- **카카오톡에서 OSM 폴백이 열리는지** (§5). 가족 한 분이 열어보면 끝난다.
- **`html{-webkit-text-size-adjust:100%}`** — 안드로이드에서 시스템 글자 크기를 키워도
  안 커진다. 부모님이 보는 앱이라 실제 문제일 수 있다. 지우면 iOS 자동 확대가
  살아나 레이아웃(특히 한 줄로 맞춘 범례)이 깨질 수 있어 손대지 않았다.
  고치려면 범례·탭·카드 폭을 다시 재야 한다.

### 자료 구멍 — 채웠지만 확인이 남은 것

여행지 48곳 주소와 맛집 51곳 전화번호를 2026-08-28 에 채웠다. 출처 2곳 이상을
대조하는 방식이었고 **전화는 41건, 주소는 41건이 확신도 높음**이다. 나머지는 아래와 같다.

**전화번호 확신도 중간 8곳** — 출처가 하나뿐이거나 지역번호와 안심번호가 갈렸다.
서귀포하르방식당 · 성읍칠십리식당 · 그리운바다 성산포 · 한림칼국수 ·
스시도모다찌 칠성점 · 제주둥지 · 옹포정미소 · 그계절.
가기 전에 한 번씩 걸어 보면 확정된다.

**안심번호(0507)로 넣은 10곳** — 산방산초가집 · 그리운바다 성산포 · 스시도모다찌 칠성점 ·
야원 · 옹포정미소 · 모슬포정미소 · 어린왕자감귤밭 · 커피스케치 · 내노키 · 루핀.
가게가 네이버 등록을 해지하면 번호가 사라진다. 인손은 휴대전화(010)뿐이고
그계절도 휴대전화다 — 유선 대표번호가 없는 가게들이다.

**주소 확신도 중간 7곳** — 지점이 아니라 구간·마을이라 대표 지점을 골라 넣은 곳들이다.
외돌개 · 사려니숲길 · 5.16도로 숲터널 · 한라생태숲 · 군산오름 · 형제해안도로 · 종달리.

### 기존 자료에서 찾아 고친 오류 세 건 (2026-08-28)

전화번호를 찾다가 나왔다. 네이버 지역검색으로 확인하고 고쳤다.

| 무엇 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 제주둥지 | `한림읍 명재로 115` · `[33.393424,126.259314]` | `한림읍 옹포4길 24` · `[33.400839,126.253399]` |
| 순천미향 제주산방산본점 | `사계남로216번길 24-7` | `사계남로216번길 24-73` |
| 선플방주할머니식당 | 상호 오기 | `선흘방주할머니식당` |
| 외돌개 | `[33.24,126.51]` | `[33.2390,126.5449]` |
| 제주현대미술관 | `[33.34,126.2703]` | `[33.3397,126.2656]` |
| 한라생태숲 | `[33.439,126.59]` | `[33.4313,126.5977]` |
| 알뜨르비행장 | `[33.205,126.267]` | `[33.2047,126.2715]` |
| 종달리 | `[33.4792,126.8668]` | `[33.4935,126.8991]` |

제주둥지는 좌표가 실제 위치에서 **991m 벗어나 있었다** — 지도에서 엉뚱한 곳을 찍고
있었다는 뜻이다. 나머지 둘은 좌표가 8m·3m 오차라 그대로 두고 글자만 고쳤다.

외돌개도 **3.2km 벗어나 있었다**(2026-08-28, 카카오 장소·주소 검색 두 경로로 확인).
`SPOT` 과 `POI` 두 곳에 같은 값이 들어 있어 함께 고쳤다.

제주현대미술관은 **440m 벗어나** 옆 건물인 김창열미술관 쪽에 찍혀 있었다.
카카오는 저지14길 35(저지리)라 하고, 등록 좌표를 역으로 물으면 한림읍 월림리가
나온다 — 앱 자체 안내문("저지리 · 김창열미술관 옆")과도 어긋났다. 이 탓에 주차장
검색이 **김창열미술관 주차장**을 잡아 두 미술관이 5m 안으로 겹쳤고, 경로를 만들 때
카카오가 그 구간을 거절해 드러났다. `SPOT`·정차지·`pk`·TSV 를 함께 고쳤다.

**아직 안 고친 것 — `AREAS` 의 제주현대미술관 지적경계 폴리곤이 `한림읍 월림리
115-72·115-73` 을 가리킨다.** 실제 지번은 저지리 2114-63 이다. 지적 자료를 다시
받아야 하는 일이라 이번에는 손대지 않았다.

2026-08-29 에 여행지 48곳·맛집 51곳을 **전수 조사했다**(`node tools/check-coords.js`).
결과는 `tools/coord-audit.tsv` 에 있다.

**방법 — 이름으로 찾지 말고 등록 주소를 좌표로 바꿔 견준다.** 이름 검색은 엉뚱한 것이
잡힌다(외돌개 → 외돌개농수산, 글라스하우스 → 강원도 고성). 주소는 사람이 적은 것이라
대개 맞고 좌표는 손으로 찍어 틀리기 쉬우니, 둘이 벌어지면 좌표를 의심한다.
등록 좌표를 역으로 물어 어느 동네에 있는지도 함께 본다 — 읍·면·리가 다르면 강한 신호다.

이 조사로 세 곳을 더 찾아 고쳤다.

- **한라생태숲** 1.1km — 좌표가 월평동에 있었다. 주소(516로 2596)도 카카오도 용강동
  산 14-1 이다. 카카오 길찾기가 이 지점을 아예 거절하던 곳이기도 하다.
- **알뜨르비행장** 420m — 좌표가 하모리에 있었다. 주소는 상모리 1670 이고 카카오도 같다.
- **종달리** 3.4km — 좌표가 내륙 농지(종달리 3987)였다. 1.5km 안에 해안 시설이 없고
  거기서 성산일출봉은 동남동인데 앱 설명은 "남쪽 수평선"이다. 카드에 적힌 주소
  (종달로5길 27, 카페 거리가 있는 마을 중심)로 옮겼다.

**남은 27곳은 오류가 아니다.** 두 갈래다.

- 26~85m — 도로명주소와 지번주소의 표기 차이일 뿐이다(오설록·산방산·화순곶자왈 등).
- 150~885m — **주소는 매표소·입구, 좌표는 명소 본체**다. 성산일출봉의 주소는 매표소
  (일출로 284-12)이고 좌표는 봉우리(성산리 1)다. 비자림·절물휴양림·천지연폭포도 같다.
  섭지코지·유민미술관·글라스하우스는 주소가 모두 `섭지코지로 107` 로 같은 한 단지라
  좌표만 다르다. **이것을 "고치면" 지도의 점이 명소에서 주차장으로 옮겨간다 — 하지 말 것.**

**좌표를 고칠 일이 또 생기면 거리를 계산해 확인할 것.** 눈으로 보면 소수점 넷째 자리
차이가 별것 아닌 듯 보이지만 실제로는 1km 다.

### 카카오 REST 키와 이동시간

편집기의 이동시간은 **카카오 길찾기**(`apis-navi.kakaomobility.com`)에서 받는다.
OSRM 은 도로 등급 태그로 미리 구운 자유주행 시간이라 정체를 모르고, 공개 데모
서버라 초당 1회·무보장·예고 없이 차단 가능하다. **이제 앱 어디에서도 OSRM 을
쓰지 않는다.**

**지도의 경로 선도 카카오에서 왔다.** 다만 실시간으로 부르지 않고 `PATHS` 에 미리
구워 두었다 — 가족은 REST 키가 없고 여행 중엔 인터넷도 없을 수 있기 때문이다.
`drawRoute` 는 그 좌표를 그리기만 하므로 네트워크도 키도 필요 없다.
**일정의 정차지를 고쳤으면 다시 만들어야 한다:**

```
KAKAO_REST_KEY=<REST 키> node tools/build-paths.js --dry   (크기만 본다)
KAKAO_REST_KEY=<REST 키> node tools/build-paths.js         (index.html 을 고친다)
```

구간마다 따로 받아 잇는다 — 카카오 경유지 한도가 5개인데 하루 정차지가 8곳인 날이
있다. 받은 점 8,074개를 20m 허용오차로 1,222개까지 줄여 넣었다(+26KB). 좌표는
`rideLL` 과 같은 규칙(주차장 우선)으로 고르므로 선과 이동시간이 어긋나지 않는다.

**편집 중에는 구운 선을 쓰지 않는다.** 2단계에서 장소를 넣거나 빼면 구워 둔 선이 옛
일정의 것이 되기 때문이다. 편집 모드이고 키가 있으면 `liveRoute()` 가 지금 정차지로
다시 받아 덮어 그린다(정차지 조합마다 캐시). 편집을 닫으면 구운 선으로 돌아온다.
키가 없으면 구운 선 그대로다 — 가족에게는 아무것도 달라지지 않는다.

**편집을 내보내 배포할 때는 반드시 다시 굽는다.** 안 그러면 배포판의 선만 옛 일정으로
남는다. `코드 복사` 로 `index.html` 을 고친 뒤 `node tools/build-paths.js` 를 돌릴 것.

**REST 키를 저장소에 넣지 않는다.** JavaScript 키와 달리 도메인 제한이 없어 소스에
박히면 남이 그대로 가져다 쓴다. 편집기(`#`)에 들어가면 키 입력칸이 나오고, 한 번
넣으면 그 브라우저의 `localStorage['jeju.naviKey']` 에만 남는다. 넣고 나면 칸이
사라지고 `실제 소요시간` 으로 바뀐다. **키가 없어도 앱은 그대로 돈다** — 이동시간만
추정값이 된다. 키는 카카오디벨로퍼스 `제주 여행 가이드`(앱 ID 1560035)의
`앱 설정 > 플랫폼 키` 에서 본다.

**좌표는 두 벌이다.** `ll` 은 지도에 점을 찍는 자리, `pk` 는 차가 서는 자리(주차장).
여행지 48곳 중 34곳에 `pk` 가 있고 `rideLL()` 이 골라 준다. **`pk` 로 `ll` 을
덮어쓰면 안 된다** — 오름 정상 대신 주차장에 점이 찍힌다. 출처는
`tools/parking-coords.tsv`, 다시 넣으려면 `node tools/apply-parking.js`.

**카카오는 좌표가 도로에서 멀면 거절한다**(`result_code 102`·`103`). 그때는 추정으로
넘어간다. 추정식은 `분 = round(직선km × 1.47 + 5)` 이고 실측 여섯 구간에서 최대
오차 31% 다. 이 식을 고치면 `tools/editor-test.js` 의 실측표도 함께 봐야 한다.

### 가족 공유 맛집·여행지 목록

여행지·맛집 탭의 **지도 아래 검색칸**에서 카카오 장소를 찾아 `+` 로 넣고, 넣은 카드의
`−` 로 뺀다. **넣고 뺀 것이 가족 모두의 기기에서 같게 보인다.**

| 어디 | 무엇 |
|---|---|
| 저장소 | 오라클 VM `140.245.67.206` 의 `/opt/jeju-api` (PM2 이름 `jeju-api`, 포트 3010) |
| 주소 | `https://hanbrook3.duckdns.org/jeju/places` — Caddy 가 `/jeju/*` 를 3010 으로 넘긴다 |
| 자료 | `/opt/jeju-api/places.json` — **덧붙이기만 하는 기록** |
| 서버 원본 | `tools/jeju-api/server.js` (의존성 없음) |

**배포판 48곳·51곳은 가족이 지울 수 없다.** 카드에 `−` 가 붙는 것은 공유 기록으로 들어온
것뿐이다. 앱은 배포판 목록을 스냅숏(`SPOT0`·`FOOD0`)해 두고 그 위에 기록을 얹어 최종
목록을 만든다. 몇 번 얹어도 결과가 같다.

**`localStorage['jeju.share']` 는 캐시일 뿐이다.** 진짜 값은 서버에 있다. 인터넷이 없으면
마지막으로 받아 둔 목록이 그대로 보이고, 그 상태에서 넣거나 빼려 하면 알려 주고 막는다.

**약속어가 소스에 들어 있다.** REST 키와 달리 이건 막을 방법이 없다 — 저장소가 공개라
소스를 읽은 사람은 목록에 무언가 넣을 수 있다. 가족 여행 목록이라 그 위험을 받아들였다.
대신 기록이 덧붙이기만 하므로 이상한 것이 들어와도 `places.json` 에서 그 줄만 지우고
`pm2 restart jeju-api` 하면 되돌아간다. 제주 밖 좌표·긴 내용·300건 초과는 서버가 거절한다.

**세 서비스가 한 VM 에 있다.** `vworld-mcp`(3000) · `korean-law-mcp`(3001) ·
`jeju-api`(3010). Caddy 는 `/jeju/*` → 3010, `/law/*` → 3001, 나머지 → 3000 순서로 본다.
**이 순서를 바꾸면 안 된다** — 나머지가 먼저 걸리면 앞의 둘이 죽는다.
설정 백업은 `/etc/caddy/Caddyfile.bak.jeju`.

**넣은 곳의 설명·메뉴는 카카오가 주지 않는다.** 이름·주소·전화·분류·카카오맵 링크가
전부다(네이버 지역 검색도 `description` 이 빈 문자열로 온다). 그래서 넣을 때 한 줄 메모를
받아 맛집은 `note`, 여행지는 `어떤 곳인가` 자리에 넣고, 카드에 카카오맵 링크를 붙였다.
**없는 것을 지어내지 않는다.**

**검색은 탭 주제에 맞는 분류만 받는다.** 여행지는 관광명소(AT4)와 문화시설(CT1),
맛집은 음식점(FD6)과 카페(CE7). 분류를 안 걸면 주차장·화장실·숙박이 섞여 나온다.
한쪽 분류만 받으면 절반이 빠진다 — 오름·폭포는 AT4 에만, 미술관·오설록은 CT1 에만 있다.

**목록이 바뀔 때 지도가 튀지 않게 해 두었다.** 점을 다시 그리려면 `drawMapFor` 를 불러야
하는데 그 안의 `fitMap` 이 화면을 처음처럼 맞춘다. `shareRedraw` 가 앞뒤로 중심과 배율을
지켰다가 되돌린다. 이걸 지우면 맛집 하나 넣을 때마다 지도가 섬 전체로 돌아간다.

**검색 결과는 지도에 따로 그린다.** 목록 점(`poiLyr`)과 다른 레이어(`foundLyr`)에 두어
`drawMapFor` 가 목록 점을 지워도 남는다. 범례에 `찾은 곳` 이 검색 중에만 나온다.
결과를 누르면 목록 카드를 누른 것과 같이 지도만 그 자리로 가고 그 점이 커진다.

**`#` 는 일정 탭에서만 보인다.** 일정 편집기 진입이라 여행지·맛집 탭에서는 쓸 일이 없다.
`searchSync()` 가 탭에 따라 숨긴다 — 지운 것이 아니니 일정 탭에서는 그대로 쓸 수 있다.

### 구조

- **base64 사진 21장이 파일의 약 74%**(1MB). 쪼개면 첫 로딩이 빨라지지만
  "한 파일" 이라는 전제가 깨진다(§2). 사용자와 합의 없이 하지 말 것.

---

## 9. 사용자와의 약속 · 취향

작업하며 확인된 것들. 어기면 되돌리라는 말을 듣는다.

- **지도 위에 무언가를 띄우지 않는다.** 조작 버튼은 전부 범례 줄에 있다. 지도 화면은
  지도만 보여야 한다. (지도 위에 남은 건 개략도 상태 배지 하나뿐이고, 실제 지도가
  뜨면 `.build.ok{display:none}` 으로 사라진다.)
- **"전체보기" 는 지도가 모바일 화면을 꽉 채우는 것**이고, 같은 버튼이 되돌리기다.
  "섬 전체" 는 별개 버튼으로 줌아웃이다. 한 번 헷갈려서 다시 만들었다.
- **처음 지도를 보여줄 때 섬이 잘리면 안 된다.** 확대되어 있으면 다시 줌아웃해야 한다.
- **하루 요약 배너를 누르면 지도가 처음 상태로 돌아간다.** 라벨 없이 배너 자체가 버튼이다.
- **핀을 누르면 그 일정의 "상단"이 나와야 한다.** 중간이 나오면 안 된다.
- **일정 탭의 조작감이 기준이다.** 여행지·맛집 탭이 다르게 움직이면 지적받는다.
  카드를 누르면 — 화면은 그대로 두고, 지도만 그 자리로 가고, 그 점을 크게 표시한다.
  지도 점을 누르면 — 그 카드를 열고 카드 상단을 지도 바로 아래 8px 로 올린다.
  자세한 것은 §11.
- **같은 내용을 두 곳에 동시에 그리지 않는다.** 지도 아래 정보 패널과 펼친 카드에
  똑같은 글이 나와 한 화면에 두 번 보인 적이 있다.
- **카카오맵 길찾기 버튼은 없앴다.** 필요 없다고 판단한 것이니 다시 넣지 말 것.
- **세로 여백을 아낀다.** 모바일 세로 길이는 정해져 있고 여백이 그걸 먹는다는 지적을
  여러 번 받았다.
- **위계를 지킨다.** 중요도가 낮은 것이 튀면 안 된다(검은 `+N` 배지가 너무 튄다는 지적).
- **글은 한국어로, 담백하게.** 커밋 메시지도 한국어다.
- 배포 후에는 **주소를 다시 알려 준다.**

---

## 10. 지난 세션들에서 한 일

| 커밋 | 내용 |
|---|---|
| `aa88244` | 지도 조작 버튼을 범례 줄로 옮김 · 핀 클릭 스크롤 수정 |
| `d70c0bf` | 개략도를 실측 자료로 재작성 · 타일 3단 폴백 |
| `0e747c7` | 서비스워커 타일 캐시 버그 수정 · 개략도 대비 조정 |
| `07f4906` | 인수인계 문서와 개략도 자료 파이프라인을 저장소에 넣음 |
| `9ce8671` | 여행지·맛집 탭의 클릭 반응을 일정 탭에 맞추고 길찾기 버튼을 걷어냄 (§11) |
| `b9f6778` | 맛집 51곳 전화번호와 여행지 48곳 주소를 채움 (§8) |
| `06bf49d` | 맛집 자료 오류 세 건을 고침 (§8) |
| `2feb143` | 지도 그리기를 함수 하나로 합침 (§12) |
| `9dfa5fd` | 탭마다 그 탭의 점만 지도에 그림 (§11) |
| `a224d1a` | 카드를 열 때 배율을 장소 크기에 맞춤 (§11) |

작업 중 확인한 사실은 §4·§5·§6·§11·§12 에 녹여 두었다. 세션이 끝나면 스크래치패드는
사라지므로 지도 파이프라인은 `tools/mapdata/` 로 옮겨 두었다.

---

## 11. 세 탭의 클릭 반응 — 일정 탭이 기준이다

한동안 탭마다 제각각이었다. 2026-08-28 에 일정 탭 기준으로 맞췄으니 되돌리지 말 것.

### 규칙

| 무엇을 누르면 | 어떻게 되어야 하나 |
|---|---|
| 목록 카드 | 화면은 **그대로 둔다**(스크롤을 건드리지 않는다) · 지도만 그 자리로 간다 · 그 점을 크게 표시한다 · 지도 아래 패널은 뜨지 않는다 |
| 지도 점·핀 | 그 카드를 열고 **카드 상단을 지도 바로 아래 8px** 로 올린다 |
| 카드가 없는 지도 점 | 그때만 지도 아래 패널을 쓴다 |

### 탭마다 보이는 점이 다르다

| 탭 | 보이는 점 | 범례 |
|---|---|---|
| 일정 | 전부 (여행지 48 · 그 밖의 명소 35 · 맛집 51) | 네 항목 |
| 여행지 | 볼거리만 (여행지 48 · 그 밖의 명소 35) | 여행지 · 그 밖의 명소 |
| 맛집 | 먹을거리만 (식당 33 · 카페 18) | 식당 · 카페 |

목록과 지도가 같은 것을 가리켜야 한다는 것이 이유다. 일정 탭만 전부 그리는 것은
그날 어디서 무엇을 먹고 보는지를 함께 봐야 하기 때문이다.

**"그 밖의 명소" 35곳은 여행지 쪽에 넣었다.** 볼거리 성격이고, 여행지 탭에서
누르면 지도 아래 패널로 안내가 나온다(목록 카드는 없다).

`drawPoi(mode)` 가 `볼거리` · `먹을거리` 두 갈래로 걸러 그리고, **범례도 같은 자리에서
맞춘다** — 그리지 않은 점을 범례가 설명하고 있으면 안 되기 때문이다.
`.legend span[hidden]{display:none}` 이 필요하다. `inline-flex` 가 `hidden` 을 이긴다.

### 배율은 장소 크기에 맞춘다 — 한 값으로 통일하지 말 것

한동안 세 탭 모두 15 하나를 썼다. 화면 폭이 1.4km 라 **주변에 아무것도 안 들어와**
제주 어디쯤인지, 근처에 뭐가 있는지 알 수 없다는 지적을 받았다.
그렇다고 한 값으로 넓히면 미술관은 점이 되고 숲길은 여전히 숲 한가운데만 보인다.

`ZOOM_THEME` 이 여행지 테마마다 배율을 정한다. 실측으로 고른 값이다.

| 배율 | 화면 폭 | 어떤 곳 |
|---|---|---|
| 14 | 2.8km | 예술·건축 (미술관·교회·박물관 8곳) · 맛집 51곳 · 식사·숙소 정차지 |
| 13 | 5.5km | 화산·지질 · 오름·폭포 · 해변·해안도로 · 이색·휴식 · 항구 |
| 12 | 11.1km | 숲·생태 · 역사·목장 (숲길·휴양림·목장·민속마을 14곳) |

해변을 12 로 했다가 13 으로 내렸다. **해변 6곳 전부 13 에서 해안선이 화면에 들어와**
위치를 알 수 있고, 12 는 그저 넓기만 했다. 반대로 목장·숲은 12 라야 전체가 담긴다.

**일정 정차지도 같은 표를 쓴다.** 이름이 여행지 목록에 있으면(30곳 중 17곳)
그 테마를 그대로 따르므로, 성산일출봉은 일정 탭에서든 여행지 탭에서든 13 이다.
나머지는 `ZOOM_TY` 로 유형을 본다 — 식사·숙소 14, 항구 13.

**개략도 상태에서는 11 하나로 간다.** 최대줌이 12 라 나눌 여지가 없다.

### 어디가 그 일을 하나

- 일정 탭 — `focusStop()` (카드 클릭) · `renderMap()` 안의 핀 `click` 핸들러 (핀 클릭)
- 여행지·맛집 탭 — `focusPoi()` (카드 클릭) · `openCard()` (지도 점 클릭)
- 공통 — `scrollUnderMap()` 이 8px 을 만든다 · `markSelAt()` 이 좌표로 점을 찾아 강조한다

### 패널을 완전히 없애지 못하는 이유

지도 점 141개 중 **35개는 카드가 없다.** `POI` 71곳 가운데 `SPOT`·`FOOD` 에 없는
것들이다(남원·표선 같은 지명 라벨, 카멜리아힐·새별오름처럼 목록에 안 넣은 명소).
이것들은 열 카드가 없으므로 패널로 보여준다. `openCard()` 가 `false` 를 돌려주면
패널로 넘어가는 구조다 — 다른 탭이거나 필터에 걸러진 항목도 같은 길로 간다.

### 예전에 뭐가 문제였나

카드를 누르면 `showInfo()` 가 **카드와 똑같은 내용**을 지도 아래 패널에 다시 그렸다.
한 화면에 같은 글이 두 번 보였다. 게다가 `mapzone.scrollIntoView({behavior:'smooth'})`
를 불렀는데 `#mapzone` 은 이미 `.stickytop` 안에 붙어 있어 **아무 일도 안 하는 코드**였고,
실제로 화면이 흔들린 원인은 패널(307px)이 끼어들며 목록을 밀어낸 것이었다.
브라우저의 스크롤 앵커링이 보정할 때도 있고 아닐 때도 있어 결과가 들쭉날쭉했다.

---

## 12. 지도는 함수 하나에서만 그린다

**지도 동작을 고칠 일이 생기면 `drawMapFor()` 한 곳만 고친다.** 그러면 세 탭에
똑같이 반영된다. 탭별로 다시 쪼개지 말 것 — 그러면 한쪽만 고쳐지는 일이 다시 생긴다.

```
drawMapFor(mode)          레이어 비우기 → pinRefs 초기화 → drawPoi()
                          → MAPPTS[mode]() 로 점 목록 → 진도 제외 → fitMap → syncZoom
                          → (일정 탭이면) drawDayPins + drawRoute
redrawMap()               지금 보고 있는 탭(curMode)으로 위를 부른다
MAPPTS                    탭마다 다른 것은 이것뿐 — 어떤 점들에 지도를 맞출 것인가
drawDayPins(stops)        일정 탭에만 있는 번호 핀
```

### 왜 합쳤나

`renderMap` · `renderSpotMap` · `renderFoodMap` 셋이 거의 같은 코드를 따로 갖고 있었고,
`curMode` 로 셋 중 하나를 고르는 **똑같은 3갈래 분기가 네 곳**에 흩어져 있었다.
그래서 한쪽만 고쳐지는 일이 실제로 여러 건 있었다.

- `map.stop()` 이 일정 탭에는 아예 없고, 여행지는 레이어 비우기 전, 맛집은 그 뒤에 있었다.
- `pinRefs` 를 일정 탭에서만 채워, 여행지·맛집으로 넘어가도 **지워진 마커 참조가 남았다**.
- 진도 제외 규칙이 맛집에만 있었다(지금은 진도 좌표가 아예 없어 아무것도 안 거르지만,
  앞으로 들어오면 세 탭이 같이 걸러야 한다).
- `initMap()` 보장이 일정 탭에만 있었다.

### 어떻게 확인하나

회귀 점검의 `지도통합` 항목이 `drawMapFor` 를 잠깐 감싸 배율을 하나 바꿔 보고,
세 탭이 모두 따라오는지 본다. 탭별로 되돌리면 `한곳수정이_세탭에` 가 `false` 가 된다.
`핀참조` 항목은 탭을 바꿀 때 `pinRefs` 가 비워지는지 본다.

---

## 13. 일정 편집기

범례 줄 끝의 작은 **`#`** 으로 들어간다. 가족이 받은 링크에는 편집할 이유가 없으므로
화면에 크게 두지 않았다. 실수로 눌러도 `원래대로` 로 완전히 되돌아간다.

설계와 이유는 [docs/superpowers/specs/2026-08-28-schedule-editor-design.md](docs/superpowers/specs/2026-08-28-schedule-editor-design.md),
1단계 구현 계획은 [docs/superpowers/plans/2026-08-28-schedule-editor-phase1.md](docs/superpowers/plans/2026-08-28-schedule-editor-phase1.md).

### 계산 블록은 DOM 을 만지지 않는다

`index.html` 안의 `/* ══════ 일정 계산 ══════ */` ~ `/* ══════ 일정 계산 끝 ══════ */`
사이에는 **순수 함수만 넣는다.** `tools/editor-test.js` 가 이 블록만 꺼내 node 에서
돌리기 때문에, DOM 이나 `localStorage` 를 만지는 순간 시험이 통째로 죽는다.
`localStorage` 를 쓰는 `editLoad` `editSave` `editClear` 와 `fetch` 를 쓰는
`rideFetch` 가 블록 밖에 있는 것은 그래서다.

```bash
node tools/editor-test.js        # 계산 시험 66건 (브라우저 없이)
```

편집 화면 점검은 `tools/editor-check.js` 를 브라우저 콘솔에 붙인다.
**서비스워커가 옛 `index.html` 을 물고 있으면 새 함수가 없다고 나온다** — 확인 전에
워커와 캐시를 지우고 새로 열 것.

### 되돌리지 말 것

- **머무는 시간은 배포판 시각에서 역산한다.** 그래야 편집기를 처음 열었을 때 계산
  결과가 지금 일정과 똑같이 나온다. 값이 갑자기 바뀌면 무엇이 내 편집이고 무엇이
  계산 오차인지 알 수 없다. 5일치 52곳 전부 일치함을 시험이 지킨다.
- **끼니는 배포판 원래 시각으로 정한다.** 계산된 시각으로 정하면 점심을 16시로 밀었을 때
  "저녁이 이르다" 로 경고가 뒤집힌다.
- **읽어낼 수 없는 운영시간·휴무 문구에는 경고하지 않는다.** 잘못된 경고는 없는 것만 못하다.
  `'매월 첫째 월요일'` `'기상 악화 시 통제'` 같은 것은 일부러 안 잡는다.
- **저장은 바뀐 값만.** 정차지 전체를 저장하면 배포판이 바뀌었을 때 옛 값이 되살아난다.
  일정 지문이 다르면 저장을 자동으로 버린다.
- **`render()` 의 `k` 는 지도 핀 번호이지 정차지 번호가 아니다.** 좌표가 없으면 `-1` 이고
  52곳 중 22곳이 그렇다. 편집은 `si`(전체 인덱스)를 쓴다. 이걸 섞으면 휴게소·체크아웃의
  편집이 통째로 어긋난다.

### 편집기가 찾아낸 것

경고 규칙을 지금 일정에 대 보니 **2일차(10월 6일 화요일) 14:00 유민미술관이
매주 화요일 휴무**에 걸린다. 편집기가 없었으면 문 앞에서 알았을 문제다.
아직 안 고쳤다.

### 아직 안 만든 것 (2단계)

일정의 장소 바꾸기 · 순서 바꾸기 · 넣기와 빼기. 설계 문서 §8 에 있다.

이동시간을 카카오 길찾기로 바꾸는 일과 주차장 좌표(`pk`)는 **끝났다**
(아래 「카카오 REST 키와 이동시간」).
