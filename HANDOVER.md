# 인수인계 — 제주 가족여행 가이드

마지막 갱신 2026-08-28 · 커밋 `9b0fa16`

---

## 1. 이게 뭔가

**2026.10.05~10.09 제주 4박 5일 가족여행 안내 웹앱.** 가족이 휴대폰으로 보는 것이 전부다.

- 배포 주소 **https://hanbrook3.github.io/jeju-trip/**
- 동행 — 아버지 · 어머니 · 아내 · 나. **부모님이 보신다**는 점이 UI 결정의 기준이다.
- 숙소 남원읍 서의로154번길 · 이동 진도↔제주 카페리
- **가족에게 카카오톡으로 공유된다.** 이게 여러 기술 결정의 원인이다(§5).

## 2. 파일

```
index.html    1,445KB   앱 전체. HTML·CSS·JS·Leaflet·사진이 한 파일에 다 들어 있다
sw.js             3KB   서비스워커 (오프라인 + 새 버전 알림)
manifest.webmanifest    PWA
icon-192.png / icon-512.png
.github/workflows/pages.yml   배포
tools/serve.js              로컬 확인용 서버
tools/regression-check.js   회귀 점검 — 손댈 때마다 돌린다
tools/mapdata/              개략도 밑그림 자료와 파이프라인 (자체 README 있음)
```

**한 파일 구조는 의도한 것이다.** 링크 하나만 던지면 열리고, 저장해서 오프라인으로도
쓸 수 있어야 하기 때문이다. 쪼개자는 제안은 이 전제를 먼저 확인하고 할 것.

## 3. `index.html` 어디에 뭐가 있나

줄 번호는 고칠 때마다 밀리므로 **`grep -n "const NAME="` 로 찾는다.**

| 이름 | 내용 | 건수 |
|---|---|---|
| `PLACES` `POI` | 지도 위 지명 라벨 / 주변 명소 점 | 10 / 71 |
| `COAST` `ISLES` `CONT` `RD` | 개략도 밑그림 (§6) | 881점 / 12섬 / 5단 / 57선 |
| `PATHS` | 하루치 주행 경로 (네트워크 없이도 같은 선을 그린다) | 5일 |
| `VEC` `VW` | 개략도 색 / 배율별 선 굵기 | — |
| `TILE_SRC` | 타일 서버 목록 (§5) | 2 |
| `AREAS` | 지적 경계 폴리곤 (브이월드 연속지적도) | 11 |
| `D` | **일정 본문.** base64 사진이 여기 들어 있다 | 5일 · 정차지 52 (좌표 있는 것 30) |
| `CAND` | 준비물 탭에 붙는 일정 연계 식당 | 9 |
| `SPOT` | 여행지 탭 (운영시간·휴무·유래·건축가·관광포인트·주소) | 48 |
| `FOOD` | 맛집 탭 (식당 33 · 카페 18 · 주소·전화) | 51 |

주요 함수 — **`drawMapFor` · `redrawMap`(세 탭의 지도를 그리는 단 하나의 길, §12)** ·
`MAPPTS`(탭마다 어떤 점에 맞출지) · `drawDayPins`(일정 탭 번호 핀) ·
`buildVector` `vecZoom`(개략도) · `mountTiles` `retryTiles`(타일) ·
`drawPoi` `declutter`(점 정리) · `focusStop` `focusPoi` `fitMap` `fitCurrent`(지도 이동) ·
`scrollUnderMap`(지도 점→카드) · `openCard`(지도 점을 누르면 그 카드를 연다) ·
`markSel` `markSelAt`(고른 점 강조) · `toggleMapFull`(전체보기) · `staticMap`(Leaflet 조차 없을 때).

배율 — `ZOOM_THEME` `ZOOM_TY` `spotZoom()`(항목마다 다르다, §11) ·
`ONE_ZOOM=14`(목록에 하나만) · `JEJU`(섬 전체 범위).

---

## 4. ⚠️ 먼저 읽을 것 — 모르면 반드시 시간을 버리는 것들

### 4.1 base64 사진 줄을 Read 하지 말 것

`index.html` 안에 사진 21장이 base64 로 박혀 있다. **한 줄이 최대 143KB.**
`Read` 로 그 줄을 열면 컨텍스트가 통째로 날아간다.

```bash
grep -n "data:image/" index.html    # 어느 줄인지 확인만
```

2026-08-28 기준 678줄(Leaflet 원본) 과 2344~2572줄 사이. 이 구간은
`sed -n` 으로도 열지 말고, 고칠 일이 있으면 **줄 단위가 아니라 앵커 문자열로** 접근한다.

### 4.2 브라우저 스크린샷이 안 된다

Browser pane 이 화면에 안 떠 있으면 `computer{action:"screenshot"}` 이 5초 만에
타임아웃난다(`tabs_select` 해도 안 될 때가 많다). 대신:

- **수치 확인** → `javascript_tool` 로 `getBoundingClientRect()` 등을 직접 재서 JSON 으로 받는다. 이게 주력이다.
- **개략도 모양 확인** → `node tools/mapdata/preview.js` 로 PNG 를 찍고 `Read` 로 연다.

`javascript_tool` 은 **30초 제한**이 있다. 대기시간을 합쳐 그걸 넘기면 실패하지만
페이지에서는 계속 돌아간다 — 결과는 뒤이어 짧은 호출로 확인하면 된다.
점검을 두세 덩어리로 쪼개는 편이 낫다.

### 4.3 파일 수정은 앵커 매칭 node 스크립트로

1.4MB 파일이라 `Edit` 도 되지만, 여러 곳을 한 번에 고칠 때는 이 패턴을 쓴다.
**하나라도 안 맞으면 아무것도 안 쓰고 죽는다**는 게 핵심이다(중간에 반쯤 고쳐진 상태가 안 생긴다).

```js
const R = [[찾을문자열, 바꿀문자열], ...];
let n = 0;
for (const [a, b] of R) {
  const c = s.split(a).length - 1;
  if (c !== 1) { console.error('실패(' + c + '건): ' + a.slice(0, 60)); process.exit(1); }
  s = s.split(a).join(b); n++;
}
fs.writeFileSync(P, s);
```

고친 뒤에는 반드시 문법 검사:

```bash
node -e "const h=require('fs').readFileSync('index.html','utf8');
(h.match(/<script>([\s\S]*?)<\/script>/g)||[]).forEach((b,i)=>{
 const c=b.replace(/^<script>/,'').replace(/<\/script>\$/,'');
 try{new Function(c);console.log(i+': OK');}catch(e){console.log(i+': '+e.message);process.exitCode=1;}});"
```

### 4.4 Bash heredoc 이 역슬래시를 먹는다

`cat > x.js <<'JS'` 안에 정규식(`\.` `\d` `\s`)을 쓰면 백슬래시가 사라진다.
정규식이 들어가는 스크립트는 **`Write` 도구로 파일을 만들거나 `Edit` 로 고친다.**
이걸로 서비스워커 정규식을 한 번 날려 먹었다.

### 4.5 Leaflet `flyTo` 는 못 믿는다

앞선 애니메이션이나 `invalidateSize()` 에 눌려 무시된다(A/B 테스트에서 9번 중 2번만 성공).
**지도 이동은 전부 `map.setView(..., {animate:false})`** 를 쓴다. 이미 그렇게 되어 있으니
되돌리지 말 것.

같은 이유로 **부드러운 스크롤(`behavior:'smooth'`)도 지도 이동에 취소된다.**
`scrollUnderMap()` 이 즉시 이동 + `requestAnimationFrame` + `setTimeout` 으로
세 번 맞추는 건 그래서다.

### 4.6 탭을 바꾼 직후 배율을 재면 거짓말이 나온다

탭 전환이 예약한 `fitMap` 이 나중에 실행되므로, 전환 직후 카드를 눌러 배율을 재면
그 항목의 배율(12~14) 이 아니라 9 로 나온다. **탭 전환 후 1.4초 이상** 기다리고 재야 한다.
회귀 점검이 실패하면 진짜 회귀인지 이 타이밍인지부터 가른다.

### 4.7 배포는 `site-deploy` 그룹의 커스텀 워크플로

GitHub Pages 의 기본 legacy 빌드가 쓰는 `pages` 동시성 그룹이 큐에 물려 멈춰 있었다.
`.github/workflows/pages.yml` 은 일부러 **`site-deploy` 라는 다른 그룹 이름**을 쓰고,
`actions/configure-pages` → `upload-pages-artifact` → `deploy-pages` 로 직접 올린다.
저장소 설정도 Pages 소스가 **GitHub Actions** 여야 한다(브랜치 빌드가 아니라).
이걸 "정리" 한다고 그룹 이름을 `pages` 로 되돌리면 배포가 다시 막힌다.

배포는 push 하면 자동이고 3~4분 걸린다. 확인:

```bash
gh run list --workflow=pages.yml --limit 1
curl -sS -o /tmp/live.html -w "%{http_code} %{size_download}\n" -H 'Cache-Control: no-cache' https://hanbrook3.github.io/jeju-trip/
```

### 4.8 서비스워커 두 가지 함정

- `e.waitUntil(update)` 를 **동기적으로** 불러야 한다. 응답을 돌려준 뒤에 부르면
  워커가 먼저 죽어 캐시 갱신이 취소된다.
- **지도 타일 호스트를 캐시 제외 규칙에 반드시 넣는다.** 타일 서버를 늘리면서
  이걸 같이 안 고쳐 OSM 타일이 캐시에 쌓인 적이 있다(OSM 이용정책 위반).
  규칙은 호스트명이 아니라 **도메인 단위**로 되어 있다 — `xdworld1/2/3` 같은 별칭까지 잡으려고.
- 캐시 내용을 강제로 비우려면 `CACHE` 이름을 올린다(현재 `jeju-trip-v3`).

---

## 5. 지도 타일 — 카카오톡 문제

**카카오톡 인앱 브라우저가 국토지리정보원(VWorld) 타일 서버를 막는다.** 가족은 카톡으로
링크를 열 테니, 이 상태가 예외가 아니라 기본에 가깝다.

대응은 3단이다. `TILE_SRC` 순서대로 시도하고 다 막히면 개략도로 내려간다.

| 순서 | 서버 | 특징 |
|---|---|---|
| 1 | `xdworld{s}.vworld.kr` (`s` = ``,`1`,`2`,`3`) | 한글 지도. 별칭 호스트로 동시 연결을 늘렸다. Referer 제한 없음 |
| 2 | `tile.openstreetmap.org` | 한글 라벨 완전. 고배율 정보량은 오히려 더 많다 |
| 3 | 개략도 (§6) | 서버가 필요 없다 |

실측으로 확인한 것:

- **OSM 은 Referer 가 없으면 조용히 죽는다.** HTTP 200 + `image/png` 로 6,987바이트짜리
  "Access blocked" 안내 이미지를 준다. `tileerror` 로 못 잡는다.
  그래서 `<meta name="referrer">` 를 `no-referrer` 에서 `strict-origin-when-cross-origin`
  으로 바꿨다. **다시 `no-referrer` 로 돌리면 OSM 폴백이 통째로 죽는다.**
- **VWorld 는 `Cache-Control` 이 없지만 ETag/Last-Modified 는 준다**(조건부 요청에 304).
  "캐시가 아예 없다"는 건 틀린 설명이다. 9타일 동시 요청 속도는 OSM 과 동률(0.38초).
- **에스리(Esri) 계열은 제주 z14 이상이 전부 빈 타일이다.** World Street / Topo /
  Light Gray 셋 다 같은 2,521바이트 `Map data not yet available` 이미지를 준다.
  바이트 수만 보면 정상처럼 보이므로 반드시 이미지를 열어 봐야 한다. 쓰지 말 것.
- **CARTO 는 키 없이 쓰면 워터마크가 도배된다.** HTTP·바이트 검사는 전부 통과한다.
- **OSM France 는 한글이 두부(□)로 깨진다.**

### 지도 배경은 배지를 눌러 바꾼다 — 일반 지도 ↔ 항공사진 (2026-08-29)

지도 왼쪽 아래 **출처 배지가 곧 단추**다. 누르면 국토지리정보원 일반 지도와
**항공사진**(`2d/Satellite`, 확장자가 `.jpeg` 다)을 오간다. 저장하지 않으므로 새로
열면 늘 일반 지도로 시작한다.

**전에는 카카오맵 배경이었으나 걷어냈다.** 카카오는 타일 주소를 공개하지 않아 제 SDK 로
지도를 따로 그리게 하고 Leaflet 이 움직일 때마다 좌표를 맞춰 줘야 했는데, **지도 두 벌을
겹쳐 돌리는 셈이라 느렸다**(사용자 지적). 항공사진은 같은 타일 방식이라 가볍고 핀·경로·
라벨이 그대로 위에 얹힌다. `kmap`·`syncKakao`·`setKakaoBg`·`vecShow` 를 지웠다 —
**`loadKakaoSdk` 는 장소 검색이 쓰므로 남겼다.**

**항공사진은 섬 전체 배율(z9)에서 먼 바다가 희게 나온다.** 브이월드 항공사진의 촬영
범위 밖이기 때문이고 고장이 아니다. 확대하면 바다까지 제대로 나온다 — 이 배경은
현장에서 확대해 볼 때 쓰는 것이라 실사용에는 걸리지 않는다.

**타일을 붙인 직후에는 화면 범위를 덜 재어 두 장만 요청하고 나머지가 빈 채로 남는다.**
`mountTiles` 끝에서 한 박자 뒤 `invalidateSize()` + `_resetView()` 로 다시 재게 해 두었다.

### ⚠ 타일 레이어에 `redraw()` 를 쓰지 말 것 (2026-08-30)

Leaflet 의 `redraw()` 는 `_tileZoom` 을 `_clampZoom(getZoom())` 으로 잡는데 **반올림을 하지
않는다.** 이 지도는 `zoomSnap:.25` 라 줌이 **8.75 같은 소수**일 수 있고, 그러면 타일 주소가
`/8.75/435/205.png` 가 되어 **어느 서버도 줄 수 없다.** 404 가 쌓여 폴백이 잘못 일어나고
끝내 **지도 배경이 통째로 사라진다.**

`_resetView()` 는 `_setView` 를 거치므로 `Math.round` 를 타 정수 줌이 보장된다.

**재현 순서** — 확대해 둔 상태에서 ① 화면 폭이 바뀌고 ② `섬 전체` 를 누르면 줌이 8.75 가
되며 ③ 화면 폭이 또 바뀌면 굳는다. 실제로는 **폰을 가로세로로 돌릴 때** 일어난다.
`map.on('resize')` 와 `mountTiles` 두 곳에 있던 `redraw()` 가 방아쇠였다.

확인하려면 `tileLyr._tileZoom` 이 정수인지 본다. 소수면 `tileLyr._resetView()` 로 복구된다.

### 인앱 브라우저를 안 거치게 해 두었다 (2026-08-28)

**카카오톡에서 링크를 열어도 모바일 브라우저로 넘어가도록 되어 있다.** 그래서 인앱
브라우저의 타일 차단은 실제로는 잘 안 겪는다. 위 3단 폴백은 그대로 두되(다른 인앱
브라우저나 설정이 다른 기기가 있을 수 있다), **이것을 이유로 지도 서비스를 바꿀 근거로
삼지는 말 것.**

그래도 개략도가 나오면(왼쪽 위에 `개략도 · 눌러 다시 시도` 배지) 타일이 막힌 것이다.

---

## 6. 개략도

타일이 막혔을 때 나오는 밑그림. **손으로 찍은 도형이 아니라 실측 자료다.**

| 요소 | 자료 |
|---|---|
| 해안선 881점 | 통계청 2018 시도경계 |
| 부속 섬 12곳 | OpenStreetMap (우도·마라도·가파도·차귀도·비양도·형제섬 등) |
| 등고선 5단 | SRTM 90m 표고를 마칭스퀘어로 딴 것 (100·300·600·1000·1400m) |
| 간선도로 57선 | OpenStreetMap. 상·하행 중복 제거함 |

만드는 법과 다시 만드는 법은 **`tools/mapdata/README.md`** 에 있다.

### 설계 원칙 — 되돌리지 말 것

- **도로에 흰 테두리를 두르지 않는다.** 밑바탕이 밝아 테두리가 뭉개져 선이 부어 보인다.
  흰 테두리는 **오늘 갈 길(경로선) 하나만** 가져야 눈에 띈다.
- **밑그림은 물러나 있어야 한다.** 도로 대비는 육지 기준 1.6~2.1:1 로 일부러 낮다.
  WCAG 3:1 미달이지만 이건 장식이 아니라 의도다 — 그 위에 여행지 48 + 맛집 51 +
  일정 핀 + 지명 라벨이 얹힌다. 일정 핀은 육지 위 6.56:1, 가장 짙은 표고대 위에서도
  3.30:1 이라 어디서도 안 묻힌다.
- **글자는 색이 아니라 흰 테두리(text-shadow 4px)로 읽힌다.** 짙은 표고대 위에서
  글자색 대비가 2.8:1 까지 떨어지지만 halo 덕에 읽힌다. 글자색을 더 진하게 하면
  밑그림이 주인공처럼 보인다.
- **배율에 따라 덜어낸다.** 지선도로는 z≥10 에서만 나온다(`vecZoom`).

### 시도했다가 뺀 것

**해변 18곳** — 실제 폭이 200~600m 라 개략도가 쓰는 배율(z9~12)에서 1~12px,
대부분 3px 남짓이라 해안선 선에 덮여 안 보였다. 최대 배율에서도 실 조각 수준.
이름난 해변은 이미 `POI` 점으로 눌러볼 수 있어 중복이었다. 다시 넣지 말 것.

**오름 삼각형 20개** — 좌표가 임의값이었고 실제 368개의 5% 도 안 돼 분포를
나타내지도 못했다. 등고선이 그 역할을 한다.

---

## 7. 확인 절차

```bash
node tools/serve.js          # http://127.0.0.1:8765
```

브라우저를 375×667 로 맞추고 `tools/regression-check.js` 를 `javascript_tool` 에 붙여 넣는다.
**대기시간 합이 26초라 30초 제한에 걸리므로 섹션 단위로 서너 번에 나눠 붙인다**
(앞 결과는 `window.__rc` 에 모인다). 기대값은 그 파일 주석에 있다.
요약하면 — 배지불일치 0 · 핀합계 30 · 지적경계 11 · 경로 5 · 길찾기 0 ·
핀클릭 간격 8px · 배율 {미술관 14, 일출봉 13, 숲길 12, 맛집 14, 일정_일출봉 13} ·
탭통일 {패널 true, 안움직임 true, 선택표시 1} ·
점클릭 {일치 true, 간격 8} · 지도통합 {거친탭 "trip,spot,food", 한곳수정이_세탭에 true} ·
핀참조 {일정 5, 여행지 0} · 점갈래(일정 48/35/51 · 여행지 48/35/0 · 맛집 0/0/51) ·
섬전체 줌 9 · 범례 높이 29±1 한 줄 · 오류 0.

**1px 오차에 속지 말 것.** 지도 높이가 213.5px 이라 `Math.round` 가 213/214 사이에서
흔들린다. 범례 높이도 28.1px 이다. 판정에 쓸 값은 "한 줄 true" 쪽이다.

**섬전체가 줌 10 으로 나오면 회귀부터 의심하지 말 것.** 나눠 붙이는 사이에 탭을 오갔거나
`전체보기` 를 켰다 껐으면 지도 크기가 아직 안 잡혀 `mapfit` 이 어중간한 값을 잡는다.
2026-08-28 에 이걸 회귀로 착각할 뻔했다 — 페이지를 새로 고치고 일정 탭에서 곧바로
`섬 전체` 만 눌러 보면 줌 9 · 다보임 true 가 나온다. §4.6 과 같은 종류의 함정이다.
개략도 `path` 가 58 대신 77 로 나오는 것도 대개 이것의 연쇄다(줌이 10 이라 z12 용 선이 나온다).

편집기를 손댔으면 `tools/editor-check.js` 도 함께 돌린다(§13).

개략도 모양은 `node tools/mapdata/preview.js` → `preview.png` 를 `Read`.

폭별 확인이 필요하면 `resize_window` 로 320 / 360 / 375 / 430 을 돌려 본다.
**320px 에서만** 범례 문구가 "그 밖의 명소"→"명소" 로 줄어든다(`.lgmore`, `max-width:344px`).

---

## 8. 남은 일

### 확인이 필요한 것

- **카카오톡에서 OSM 폴백이 열리는지** (§5). 가족 한 분이 열어보면 끝난다.
- **`html{-webkit-text-size-adjust:100%}`** — 안드로이드에서 시스템 글자 크기를 키워도
  안 커진다. 부모님이 보는 앱이라 실제 문제일 수 있다. 지우면 iOS 자동 확대가
  살아나 레이아웃(특히 한 줄로 맞춘 범례)이 깨질 수 있어 손대지 않았다.
  고치려면 범례·탭·카드 폭을 다시 재야 한다.

### 자료 구멍 — 채웠지만 확인이 남은 것

여행지 48곳 주소와 맛집 51곳 전화번호를 2026-08-28 에 채웠다. 출처 2곳 이상을
대조하는 방식이었고 **전화는 41건, 주소는 41건이 확신도 높음**이다. 나머지는 아래와 같다.

**전화번호 확신도 중간 8곳** — 출처가 하나뿐이거나 지역번호와 안심번호가 갈렸다.
서귀포하르방식당 · 성읍칠십리식당 · 그리운바다 성산포 · 한림칼국수 ·
스시도모다찌 칠성점 · 제주둥지 · 옹포정미소 · 그계절.
가기 전에 한 번씩 걸어 보면 확정된다.

**안심번호(0507)로 넣은 10곳** — 산방산초가집 · 그리운바다 성산포 · 스시도모다찌 칠성점 ·
야원 · 옹포정미소 · 모슬포정미소 · 어린왕자감귤밭 · 커피스케치 · 내노키 · 루핀.
가게가 네이버 등록을 해지하면 번호가 사라진다. 인손은 휴대전화(010)뿐이고
그계절도 휴대전화다 — 유선 대표번호가 없는 가게들이다.

**주소 확신도 중간 7곳** — 지점이 아니라 구간·마을이라 대표 지점을 골라 넣은 곳들이다.
외돌개 · 사려니숲길 · 5.16도로 숲터널 · 한라생태숲 · 군산오름 · 형제해안도로 · 종달리.

### 기존 자료에서 찾아 고친 오류 세 건 (2026-08-28)

전화번호를 찾다가 나왔다. 네이버 지역검색으로 확인하고 고쳤다.

| 무엇 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 제주둥지 | `한림읍 명재로 115` · `[33.393424,126.259314]` | `한림읍 옹포4길 24` · `[33.400839,126.253399]` |
| 순천미향 제주산방산본점 | `사계남로216번길 24-7` | `사계남로216번길 24-73` |
| 선플방주할머니식당 | 상호 오기 | `선흘방주할머니식당` |
| 외돌개 | `[33.24,126.51]` | `[33.2390,126.5449]` |
| 제주현대미술관 | `[33.34,126.2703]` | `[33.3397,126.2656]` |
| 한라생태숲 | `[33.439,126.59]` | `[33.4313,126.5977]` |
| 알뜨르비행장 | `[33.205,126.267]` | `[33.2047,126.2715]` |
| 종달리 | `[33.4792,126.8668]` | `[33.4935,126.8991]` |

제주둥지는 좌표가 실제 위치에서 **991m 벗어나 있었다** — 지도에서 엉뚱한 곳을 찍고
있었다는 뜻이다. 나머지 둘은 좌표가 8m·3m 오차라 그대로 두고 글자만 고쳤다.

외돌개도 **3.2km 벗어나 있었다**(2026-08-28, 카카오 장소·주소 검색 두 경로로 확인).
`SPOT` 과 `POI` 두 곳에 같은 값이 들어 있어 함께 고쳤다.

제주현대미술관은 **440m 벗어나** 옆 건물인 김창열미술관 쪽에 찍혀 있었다.
카카오는 저지14길 35(저지리)라 하고, 등록 좌표를 역으로 물으면 한림읍 월림리가
나온다 — 앱 자체 안내문("저지리 · 김창열미술관 옆")과도 어긋났다. 이 탓에 주차장
검색이 **김창열미술관 주차장**을 잡아 두 미술관이 5m 안으로 겹쳤고, 경로를 만들 때
카카오가 그 구간을 거절해 드러났다. `SPOT`·정차지·`pk`·TSV 를 함께 고쳤다.

**지적경계도 함께 고쳤다.** `AREAS` 의 제주현대미술관 폴리곤 둘이 `한림읍 월림리
115-72·115-73` 을 가리켜 실제 미술관에서 439m·456m 떨어진 곳을 그리고 있었다.
좌표 440m 오류와 같은 뿌리다. 브이월드에서 실제 필지 `한경면 저지리 2114-63`
(PNU 5011031030121140063)의 경계를 받아 **한 항목으로 합쳤다** — 미술관은 한 필지
위에 있다. 폴리곤 중심이 미술관에서 39m 다(전 439m). `AREAS` 가 11개에서 10개가
되었으니 회귀 점검 기대값도 함께 고쳤다.

**김창열미술관의 `한림읍 월림리 115-23` 은 맞다.** 저지문화예술인마을이 한림읍과
한경면에 걸쳐 있어 두 미술관의 소재 읍면이 다르다 — 주소만 보고 "같은 마을이니
같은 읍면"이라고 고치면 안 된다. 나머지 여덟도 다시 재봤고 모두 제 필지 위에 있다.
송악산 폴리곤 중심이 정차지에서 574m 인 것은 산 전체가 한 필지(상모리 산 2)여서다.

2026-08-29 에 여행지 48곳·맛집 51곳을 **전수 조사했다**(`node tools/check-coords.js`).
결과는 `tools/coord-audit.tsv` 에 있다.

**방법 — 이름으로 찾지 말고 등록 주소를 좌표로 바꿔 견준다.** 이름 검색은 엉뚱한 것이
잡힌다(외돌개 → 외돌개농수산, 글라스하우스 → 강원도 고성). 주소는 사람이 적은 것이라
대개 맞고 좌표는 손으로 찍어 틀리기 쉬우니, 둘이 벌어지면 좌표를 의심한다.
등록 좌표를 역으로 물어 어느 동네에 있는지도 함께 본다 — 읍·면·리가 다르면 강한 신호다.

이 조사로 세 곳을 더 찾아 고쳤다.

- **한라생태숲** 1.1km — 좌표가 월평동에 있었다. 주소(516로 2596)도 카카오도 용강동
  산 14-1 이다. 카카오 길찾기가 이 지점을 아예 거절하던 곳이기도 하다.
- **알뜨르비행장** 420m — 좌표가 하모리에 있었다. 주소는 상모리 1670 이고 카카오도 같다.
- **종달리** 3.4km — 좌표가 내륙 농지(종달리 3987)였다. 1.5km 안에 해안 시설이 없고
  거기서 성산일출봉은 동남동인데 앱 설명은 "남쪽 수평선"이다. 카드에 적힌 주소
  (종달로5길 27, 카페 거리가 있는 마을 중심)로 옮겼다.

**남은 27곳은 오류가 아니다.** 두 갈래다.

- 26~85m — 도로명주소와 지번주소의 표기 차이일 뿐이다(오설록·산방산·화순곶자왈 등).
- 150~885m — **주소는 매표소·입구, 좌표는 명소 본체**다. 성산일출봉의 주소는 매표소
  (일출로 284-12)이고 좌표는 봉우리(성산리 1)다. 비자림·절물휴양림·천지연폭포도 같다.
  섭지코지·유민미술관·글라스하우스는 주소가 모두 `섭지코지로 107` 로 같은 한 단지라
  좌표만 다르다. **이것을 "고치면" 지도의 점이 명소에서 주차장으로 옮겨간다 — 하지 말 것.**

**좌표를 고칠 일이 또 생기면 거리를 계산해 확인할 것.** 눈으로 보면 소수점 넷째 자리
차이가 별것 아닌 듯 보이지만 실제로는 1km 다.

### 카카오 REST 키와 이동시간

편집기의 이동시간은 **카카오 길찾기**(`apis-navi.kakaomobility.com`)에서 받는다.
OSRM 은 도로 등급 태그로 미리 구운 자유주행 시간이라 정체를 모르고, 공개 데모
서버라 초당 1회·무보장·예고 없이 차단 가능하다. **이제 앱 어디에서도 OSRM 을
쓰지 않는다.**

**지도의 경로 선도 카카오에서 왔다.** 다만 실시간으로 부르지 않고 `PATHS` 에 미리
구워 두었다 — 가족은 REST 키가 없고 여행 중엔 인터넷도 없을 수 있기 때문이다.
`drawRoute` 는 그 좌표를 그리기만 하므로 네트워크도 키도 필요 없다.
**일정의 정차지를 고쳤으면 다시 만들어야 한다:**

```
KAKAO_REST_KEY=<REST 키> node tools/build-paths.js --dry   (크기만 본다)
KAKAO_REST_KEY=<REST 키> node tools/build-paths.js         (index.html 을 고친다)
```

구간마다 따로 받아 잇는다 — 카카오 경유지 한도가 5개인데 하루 정차지가 8곳인 날이
있다. 받은 점 8,074개를 20m 허용오차로 1,222개까지 줄여 넣었다(+26KB). 좌표는
`rideLL` 과 같은 규칙(주차장 우선)으로 고르므로 선과 이동시간이 어긋나지 않는다.

**편집 중에는 구운 선을 쓰지 않는다.** 2단계에서 장소를 넣거나 빼면 구워 둔 선이 옛
일정의 것이 되기 때문이다. 편집 모드이고 키가 있으면 `liveRoute()` 가 지금 정차지로
다시 받아 덮어 그린다(정차지 조합마다 캐시). 편집을 닫으면 구운 선으로 돌아온다.
키가 없으면 구운 선 그대로다 — 가족에게는 아무것도 달라지지 않는다.

**편집을 내보내 배포할 때는 반드시 다시 굽는다.** 안 그러면 배포판의 선만 옛 일정으로
남는다. `코드 복사` 로 `index.html` 을 고친 뒤 `node tools/build-paths.js` 를 돌릴 것.

**선이 정차지까지 닿지 않는 곳이 있다.** 카카오 도로망에 없는 자리는 가장 가까운 도로에
붙는다 — 성산일출봉 주차장은 441m, 1100고지 습지는 262m 떨어진 도로에서 선이 끊긴다.
고장이 아니다. 경로가 낡았는지 볼 때는 이 둘을 빼고 보거나, 정차지 순서대로 지나가는지로
판단할 것.

**구간 실측을 구워 두었다** (2026-08-29). `RIDE` 에 15구간의 `[분, km, 통행료]` 가 들어
있고 `rideNow` 가 추정보다 먼저 쓴다. **가족은 키가 없어도 실제 도로 시간을 본다** —
전에는 추정식(오차 최대 31%)으로 떨어졌다. 1일차 제주항→방목지가 추정 23분에서 실측
31분으로 바뀌었다.

**여행 당일 그 시각에 출발한다고 보고 받는다.** 일반 길찾기(`/v1/directions`)는 "지금"
기준이라 새벽에 갈 길을 저녁 교통으로 재는 셈이 된다. **미래 운행 엔드포인트**
(`/v1/future/directions`)에 `departure_time=yyyyMMddHHmm` 을 주면 그 시각의 예측을 준다 —
제주항→방목지가 지금 기준 32분인데 10/5 05:55 출발이면 31분, 사려니→1100고지는 49분에서
47분이다. **일반 엔드포인트에 `departure_time` 을 붙여도 무시된다**(값이 그대로 나온다).
가짜 시간을 넣으면 실제로 다녀 보았을 때 일정이 엉망이 되므로 이렇게 한다.

출발 시각은 **두 번 돌려 맞춘다.** 정차지에 적힌 시각은 도착 시각이라 실제 출발은 그보다
늦다. 1차로 어림한 이동시간을 다음 정차지 도착 시각에서 빼면 출발 시각이 나오고, 그
시각으로 다시 받는다.

다시 만들려면:

```bash
KAKAO_REST_KEY=$(cat tools/.kakao-key) node tools/build-rides.js --dry   # 밀리는 곳만 본다
KAKAO_REST_KEY=$(cat tools/.kakao-key) node tools/build-rides.js
```

`--dry` 가 **실측이 지금 일정보다 오래 걸리는 곳**을 먼저 알려 준다. 넣으면 그만큼
시각이 밀리기 때문이다(2026-08-29 에는 한 곳도 없었다). 정차지를 고쳤으면 다시 굽는다.

**부제(`sub`)는 이 스크립트가 건드리지 않는다.** 부제의 시간 표현은 뜻이 제각각이라
(`왕복 1시간 30분`=체류, `항해 2시간`=배, `385.4km`=여러 구간 합) 자동으로 갈아 끼우면
틀린 값이 들어간다. 통행료도 제주 안은 전부 0원이고, 1일차 육지 구간(13,800원)은
좌표가 없어 받을 수 없다.

**실측에 없는 조합은 편집 줄에 `어림` 이라고 붙는다.** 장소를 옮기면 구워 둔 조합에서
벗어나기 때문이다. **키가 있으면 `warmRides` 가 그때 받아 채우고 `어림` 이 사라진다** —
이때도 여행 당일 그 시각으로 묻는다(`rideFetch(a,b,떠날때(day,시각))`). 시각은 계산된
출발 시각(`dayPlan().depart`)을 쓰고, 없으면 그 정차지에 적힌 시각으로 물러선다.

**받아 온 값은 거리·통행료까지 `rideFull` 에 담고 `rideInfo` 가 구워 둔 것보다 먼저 쓴다.**
분만 담으면 화면이 직선 거리를 쓰고 받아 왔는데도 `어림` 이라고 붙는다. 실제로 그랬다 —
제주항→비자림이 어림 43분·직선 25.7km 로 나오다가, 받고 나서 49분·도로 33km 가 되었다.
**어림과 실측이 6분이나 벌어졌다.**

**`liveRoute`(편집 중 경로 선)는 일반 엔드포인트를 그대로 쓴다.** 선의 **모양**은 출발
시각과 무관하기 때문이다. 시각에 민감한 것은 이동시간뿐이고 그쪽은 `rideFetch` 가 맡는다.

**REST 키를 저장소에 넣지 않는다.** JavaScript 키와 달리 도메인 제한이 없어 소스에
박히면 남이 그대로 가져다 쓴다. 편집기(`#`)에 들어가면 키 입력칸이 나오고, 한 번
넣으면 그 브라우저의 `localStorage['jeju.naviKey']` 에만 남는다. 넣고 나면 칸이
사라지고 `실제 소요시간` 으로 바뀐다. **키가 없어도 앱은 그대로 돈다** — 이동시간만
추정값이 된다. 키는 카카오디벨로퍼스 `제주 여행 가이드`(앱 ID 1560035)의
`앱 설정 > 플랫폼 키` 에서 본다.

**좌표는 두 벌이다.** `ll` 은 지도에 점을 찍는 자리, `pk` 는 차가 서는 자리(주차장).
여행지 48곳 중 34곳에 `pk` 가 있고 `rideLL()` 이 골라 준다. **`pk` 로 `ll` 을
덮어쓰면 안 된다** — 오름 정상 대신 주차장에 점이 찍힌다. 출처는
`tools/parking-coords.tsv`, 다시 넣으려면 `node tools/apply-parking.js`.

**카카오는 좌표가 도로에서 멀면 거절한다**(`result_code 102`·`103`). 그때는 추정으로
넘어간다. 추정식은 `분 = round(직선km × 1.47 + 5)` 이고 실측 여섯 구간에서 최대
오차 31% 다. 이 식을 고치면 `tools/editor-test.js` 의 실측표도 함께 봐야 한다.

### 지도 배경 바꾸기

지도 왼쪽 아래 배지를 누르면 **국토지리정보원 ↔ 카카오맵** 이 바뀐다. 기본은
국토지리정보원이다.

**카카오 타일을 Leaflet 에 직접 쓰면 안 된다.** 카카오 공식 답변이 "다른 라이브러리
사용은 지원하지 않습니다" 이고, 약관 제11조(안내하는 방법 이외의 접근)를 든다.
실제로 위반 사이트를 찾아 조치하고 있다. 카카오가 타일 주소를 공개하지도 않는다.

그래서 **카카오가 제 SDK 로 지도를 그리게 하고 화면만 맞춘다.** `#kmap` 이 Leaflet 판들
뒤에 깔리고, 핀·경로·정보 패널은 지금 그대로 그 위에 그려진다. Leaflet 이 움직일 때마다
`syncKakao()` 가 중심과 배율을 옮긴다.

**배율 대응은 실측으로 맞췄다 — 카카오 레벨 = 20 − Leaflet 줌.** (레벨 1↔줌 19,
레벨 14↔줌 6). 이 앱의 최대 줌 17 은 레벨 3 이다. 지도 이동이 전부 `animate:false`
라서(§4) 두 지도가 어긋나 보이는 일이 없다 — 이 규칙을 깨면 배경만 늦게 따라온다.

**카카오 배경일 때는 개략도를 숨긴다.** 개략도는 `vecpane`(z-index 150)에 늘 그려져
있고 타일이 덮고 있을 뿐이라, 타일을 걷어내면 카카오 위로 드러난다. 같은 이유로
`buildState` 를 `ok` 로 두어 "타일이 안 뜬다"는 폴백이 개략도를 켜지 않게 하고,
개략도가 걸어 둔 배율 상한 12 도 17 로 되돌린다.

**카카오 배경은 인터넷이 있어야 한다.** 못 부르면 배지에 알리고 그대로 둔다.
가족이 오프라인에서 여는 기본 상태는 국토지리정보원이고, 그때는 지금과 똑같다.

### 가족 공유 맛집·여행지 목록

여행지·맛집 탭의 **지도 아래 검색칸**에서 카카오 장소를 찾아 `+` 로 넣고, 넣은 카드의
`−` 로 뺀다. **넣고 뺀 것이 가족 모두의 기기에서 같게 보인다.**

| 어디 | 무엇 |
|---|---|
| 저장소 | 오라클 VM `140.245.67.206` 의 `/opt/jeju-api` (PM2 이름 `jeju-api`, 포트 3010) |
| 주소 | `https://hanbrook3.duckdns.org/jeju/places` — Caddy 가 `/jeju/*` 를 3010 으로 넘긴다 |
| 자료 | `/opt/jeju-api/places.json` — **덧붙이기만 하는 기록** |
| 서버 원본 | `tools/jeju-api/server.js` (의존성 없음) |

**배포판 48곳·51곳은 가족이 지울 수 없다.** 카드에 `−` 가 붙는 것은 공유 기록으로 들어온
것뿐이다. 앱은 배포판 목록을 스냅숏(`SPOT0`·`FOOD0`)해 두고 그 위에 기록을 얹어 최종
목록을 만든다. 몇 번 얹어도 결과가 같다.

**`localStorage['jeju.share']` 는 캐시일 뿐이다.** 진짜 값은 서버에 있다. 인터넷이 없으면
마지막으로 받아 둔 목록이 그대로 보이고, 그 상태에서 넣거나 빼려 하면 알려 주고 막는다.

**약속어가 소스에 들어 있다.** REST 키와 달리 이건 막을 방법이 없다 — 저장소가 공개라
소스를 읽은 사람은 목록에 무언가 넣을 수 있다. 가족 여행 목록이라 그 위험을 받아들였다.
대신 기록이 덧붙이기만 하므로 이상한 것이 들어와도 `places.json` 에서 그 줄만 지우고
`pm2 restart jeju-api` 하면 되돌아간다. 제주 밖 좌표·긴 내용·300건 초과는 서버가 거절한다.

**세 서비스가 한 VM 에 있다.** `vworld-mcp`(3000) · `korean-law-mcp`(3001) ·
`jeju-api`(3010). Caddy 는 `/jeju/*` → 3010, `/law/*` → 3001, 나머지 → 3000 순서로 본다.
**이 순서를 바꾸면 안 된다** — 나머지가 먼저 걸리면 앞의 둘이 죽는다.
설정 백업은 `/etc/caddy/Caddyfile.bak.jeju`.

**넣은 곳의 설명·메뉴는 카카오가 주지 않는다.** 이름·주소·전화·분류·카카오맵 링크가
전부다(네이버 지역 검색도 `description` 이 빈 문자열로 온다). 그래서 넣을 때 한 줄 메모를
받아 맛집은 `note`, 여행지는 `어떤 곳인가` 자리에 넣고, 카드에 카카오맵 링크를 붙였다.
**없는 것을 지어내지 않는다.**

**검색은 탭 주제에 맞는 분류만 받는다.** 여행지는 관광명소(AT4)와 문화시설(CT1),
맛집은 음식점(FD6)과 카페(CE7). 분류를 안 걸면 주차장·화장실·숙박이 섞여 나온다.
한쪽 분류만 받으면 절반이 빠진다 — 오름·폭포는 AT4 에만, 미술관·오설록은 CT1 에만 있다.

**목록이 바뀔 때 지도가 튀지 않게 해 두었다.** 점을 다시 그리려면 `drawMapFor` 를 불러야
하는데 그 안의 `fitMap` 이 화면을 처음처럼 맞춘다. `shareRedraw` 가 앞뒤로 중심과 배율을
지켰다가 되돌린다. 이걸 지우면 맛집 하나 넣을 때마다 지도가 섬 전체로 돌아간다.

**검색 결과는 지도에 따로 그린다.** 목록 점(`poiLyr`)과 다른 레이어(`foundLyr`)에 두어
`drawMapFor` 가 목록 점을 지워도 남는다. 범례에 `찾은 곳` 이 검색 중에만 나온다.
결과를 누르면 목록 카드를 누른 것과 같이 지도만 그 자리로 가고 그 점이 커진다.

**`#` 는 일정 탭에서만 보인다.** 일정 편집기 진입이라 여행지·맛집 탭에서는 쓸 일이 없다.
`searchSync()` 가 탭에 따라 숨긴다 — 지운 것이 아니니 일정 탭에서는 그대로 쓸 수 있다.

**`prompt`·`confirm`·`alert` 에 기대지 말 것.** 이 앱은 카톡으로 공유되는데 인앱
브라우저가 대화창을 막는다. 막히면 `null` 을 주는 게 아니라 **예외를 던져서** 그 자리에서
코드가 죽는다 — 버튼을 눌러도 아무 일도 일어나지 않고 오류 표시도 없다. 실제로 넣기
버튼이 이것 때문에 먹통이었다.

그래서 넣기는 `+` 를 누르면 결과 자리에 작은 입력칸(`.saddbox`)이 열리고, 빼기는 `−` 를
**두 번** 누르게 했다(첫 번째에 "뺄까요?" 로 바뀌고 4초 뒤 되돌아간다). 이름도 물어보지
않고 넣기 칸에서 처음 한 번만 받는다.

**일정 편집기도 같은 방식으로 고쳤다** (2026-08-29). 장소 직접 입력은 찾기 칸에 친 글자를
그대로 이름으로 쓰고, 빼기는 두 번 누르게 했다. **이제 앱 전체에 `prompt`·`confirm`·
`alert` 가 하나도 없다** — `grep -c "prompt(|confirm(|alert(" index.html` 이 0 이다.

**가족이 넣은 곳은 한눈에 구분된다.** 카드 왼쪽에 띠(`.fcard.newone`)가 붙고 이름 옆에
`새로 넣음` 과 넣은 사람이 나온다. 배포판 48곳·51곳에는 아무것도 붙지 않는다.

### 구조

- **base64 사진 21장이 파일의 약 74%**(1MB). 쪼개면 첫 로딩이 빨라지지만
  "한 파일" 이라는 전제가 깨진다(§2). 사용자와 합의 없이 하지 말 것.

---

## 9. 사용자와의 약속 · 취향

작업하며 확인된 것들. 어기면 되돌리라는 말을 듣는다.

- **지도 위에 무언가를 띄우지 않는다.** 조작 버튼은 전부 범례 줄에 있다. 지도 화면은
  지도만 보여야 한다. (지도 위에 남은 건 개략도 상태 배지 하나뿐이고, 실제 지도가
  뜨면 `.build.ok{display:none}` 으로 사라진다.)
- **"전체보기" 는 지도가 모바일 화면을 꽉 채우는 것**이고, 같은 버튼이 되돌리기다.
  "섬 전체" 는 별개 버튼으로 줌아웃이다. 한 번 헷갈려서 다시 만들었다.
- **처음 지도를 보여줄 때 섬이 잘리면 안 된다.** 확대되어 있으면 다시 줌아웃해야 한다.
- **하루 요약 배너를 누르면 지도가 처음 상태로 돌아간다.** 라벨 없이 배너 자체가 버튼이다.
- **핀을 누르면 그 일정의 "상단"이 나와야 한다.** 중간이 나오면 안 된다.
- **일정 탭의 조작감이 기준이다.** 여행지·맛집 탭이 다르게 움직이면 지적받는다.
  카드를 누르면 — 화면은 그대로 두고, 지도만 그 자리로 가고, 그 점을 크게 표시한다.
  지도 점을 누르면 — 그 카드를 열고 카드 상단을 지도 바로 아래 8px 로 올린다.
  자세한 것은 §11.
- **같은 내용을 두 곳에 동시에 그리지 않는다.** 지도 아래 정보 패널과 펼친 카드에
  똑같은 글이 나와 한 화면에 두 번 보인 적이 있다.
- **카카오맵 길찾기 버튼은 없앴다.** 필요 없다고 판단한 것이니 다시 넣지 말 것.
- **세로 여백을 아낀다.** 모바일 세로 길이는 정해져 있고 여백이 그걸 먹는다는 지적을
  여러 번 받았다.
- **위계를 지킨다.** 중요도가 낮은 것이 튀면 안 된다(검은 `+N` 배지가 너무 튄다는 지적).
- **글은 한국어로, 담백하게.** 커밋 메시지도 한국어다.
- 배포 후에는 **주소를 다시 알려 준다.**

---

## 10. 지난 세션들에서 한 일

| 커밋 | 내용 |
|---|---|
| `aa88244` | 지도 조작 버튼을 범례 줄로 옮김 · 핀 클릭 스크롤 수정 |
| `d70c0bf` | 개략도를 실측 자료로 재작성 · 타일 3단 폴백 |
| `0e747c7` | 서비스워커 타일 캐시 버그 수정 · 개략도 대비 조정 |
| `07f4906` | 인수인계 문서와 개략도 자료 파이프라인을 저장소에 넣음 |
| `9ce8671` | 여행지·맛집 탭의 클릭 반응을 일정 탭에 맞추고 길찾기 버튼을 걷어냄 (§11) |
| `b9f6778` | 맛집 51곳 전화번호와 여행지 48곳 주소를 채움 (§8) |
| `06bf49d` | 맛집 자료 오류 세 건을 고침 (§8) |
| `2feb143` | 지도 그리기를 함수 하나로 합침 (§12) |
| `9dfa5fd` | 탭마다 그 탭의 점만 지도에 그림 (§11) |
| `a224d1a` | 카드를 열 때 배율을 장소 크기에 맞춤 (§11) |
| — | 일정 편집기 1단계 — 계산·경고·저장·코드 복사 (§13) |
| `b72dfa5` | 2·3일차를 맞바꿔 유민미술관 휴무를 피함 (§13) |
| `86082c2` `897b209` | 정차지의 장소를 다른 곳으로 바꿀 수 있게 함 (§13) |
| `9b0fa16` | 지도 배경을 카카오맵으로 바꿀 수 있게 함 |

**다음 세션은 §14 부터 보면 된다.**

작업 중 확인한 사실은 §4·§5·§6·§11·§12·§13 에 녹여 두었다. 세션이 끝나면 스크래치패드는
사라지므로 지도 파이프라인은 `tools/mapdata/` 로 옮겨 두었다.

---

## 11. 세 탭의 클릭 반응 — 일정 탭이 기준이다

한동안 탭마다 제각각이었다. 2026-08-28 에 일정 탭 기준으로 맞췄으니 되돌리지 말 것.

### 규칙

| 무엇을 누르면 | 어떻게 되어야 하나 |
|---|---|
| 목록 카드 | 화면은 **그대로 둔다**(스크롤을 건드리지 않는다) · 지도만 그 자리로 간다 · 그 점을 크게 표시한다 · 지도 아래 패널은 뜨지 않는다 |
| 지도 점·핀 | 그 카드를 열고 **카드 상단을 지도 바로 아래 8px** 로 올린다 |
| 카드가 없는 지도 점 | 그때만 지도 아래 패널을 쓴다 |

### 탭마다 보이는 점이 다르다

| 탭 | 보이는 점 | 범례 |
|---|---|---|
| 일정 | 전부 (여행지 48 · 그 밖의 명소 35 · 맛집 51) | 네 항목 |
| 여행지 | 볼거리만 (여행지 48 · 그 밖의 명소 35) | 여행지 · 그 밖의 명소 |
| 맛집 | 먹을거리만 (식당 33 · 카페 18) | 식당 · 카페 |

목록과 지도가 같은 것을 가리켜야 한다는 것이 이유다. 일정 탭만 전부 그리는 것은
그날 어디서 무엇을 먹고 보는지를 함께 봐야 하기 때문이다.

**"그 밖의 명소" 35곳은 여행지 쪽에 넣었다.** 볼거리 성격이고, 여행지 탭에서
누르면 지도 아래 패널로 안내가 나온다(목록 카드는 없다).

`drawPoi(mode)` 가 `볼거리` · `먹을거리` 두 갈래로 걸러 그리고, **범례도 같은 자리에서
맞춘다** — 그리지 않은 점을 범례가 설명하고 있으면 안 되기 때문이다.
`.legend span[hidden]{display:none}` 이 필요하다. `inline-flex` 가 `hidden` 을 이긴다.

### 배율은 장소 크기에 맞춘다 — 한 값으로 통일하지 말 것

한동안 세 탭 모두 15 하나를 썼다. 화면 폭이 1.4km 라 **주변에 아무것도 안 들어와**
제주 어디쯤인지, 근처에 뭐가 있는지 알 수 없다는 지적을 받았다.
그렇다고 한 값으로 넓히면 미술관은 점이 되고 숲길은 여전히 숲 한가운데만 보인다.

`ZOOM_THEME` 이 여행지 테마마다 배율을 정한다. 실측으로 고른 값이다.

| 배율 | 화면 폭 | 어떤 곳 |
|---|---|---|
| 14 | 2.8km | 예술·건축 (미술관·교회·박물관 8곳) · 맛집 51곳 · 식사·숙소 정차지 |
| 13 | 5.5km | 화산·지질 · 오름·폭포 · 해변·해안도로 · 이색·휴식 · 항구 |
| 12 | 11.1km | 숲·생태 · 역사·목장 (숲길·휴양림·목장·민속마을 14곳) |

해변을 12 로 했다가 13 으로 내렸다. **해변 6곳 전부 13 에서 해안선이 화면에 들어와**
위치를 알 수 있고, 12 는 그저 넓기만 했다. 반대로 목장·숲은 12 라야 전체가 담긴다.

**일정 정차지도 같은 표를 쓴다.** 이름이 여행지 목록에 있으면(30곳 중 17곳)
그 테마를 그대로 따르므로, 성산일출봉은 일정 탭에서든 여행지 탭에서든 13 이다.
나머지는 `ZOOM_TY` 로 유형을 본다 — 식사·숙소 14, 항구 13.

**개략도 상태에서는 11 하나로 간다.** 최대줌이 12 라 나눌 여지가 없다.

### 어디가 그 일을 하나

- 일정 탭 — `focusStop()` (카드 클릭) · `renderMap()` 안의 핀 `click` 핸들러 (핀 클릭)
- 여행지·맛집 탭 — `focusPoi()` (카드 클릭) · `openCard()` (지도 점 클릭)
- 공통 — `scrollUnderMap()` 이 8px 을 만든다 · `markSelAt()` 이 좌표로 점을 찾아 강조한다

### 패널을 완전히 없애지 못하는 이유

지도 점 141개 중 **35개는 카드가 없다.** `POI` 71곳 가운데 `SPOT`·`FOOD` 에 없는
것들이다(남원·표선 같은 지명 라벨, 카멜리아힐·새별오름처럼 목록에 안 넣은 명소).
이것들은 열 카드가 없으므로 패널로 보여준다. `openCard()` 가 `false` 를 돌려주면
패널로 넘어가는 구조다 — 다른 탭이거나 필터에 걸러진 항목도 같은 길로 간다.

### 예전에 뭐가 문제였나

카드를 누르면 `showInfo()` 가 **카드와 똑같은 내용**을 지도 아래 패널에 다시 그렸다.
한 화면에 같은 글이 두 번 보였다. 게다가 `mapzone.scrollIntoView({behavior:'smooth'})`
를 불렀는데 `#mapzone` 은 이미 `.stickytop` 안에 붙어 있어 **아무 일도 안 하는 코드**였고,
실제로 화면이 흔들린 원인은 패널(307px)이 끼어들며 목록을 밀어낸 것이었다.
브라우저의 스크롤 앵커링이 보정할 때도 있고 아닐 때도 있어 결과가 들쭉날쭉했다.

---

## 12. 지도는 함수 하나에서만 그린다

**지도 동작을 고칠 일이 생기면 `drawMapFor()` 한 곳만 고친다.** 그러면 세 탭에
똑같이 반영된다. 탭별로 다시 쪼개지 말 것 — 그러면 한쪽만 고쳐지는 일이 다시 생긴다.

```
drawMapFor(mode)          레이어 비우기 → pinRefs 초기화 → drawPoi()
                          → MAPPTS[mode]() 로 점 목록 → 진도 제외 → fitMap → syncZoom
                          → (일정 탭이면) drawDayPins + drawRoute
redrawMap()               지금 보고 있는 탭(curMode)으로 위를 부른다
MAPPTS                    탭마다 다른 것은 이것뿐 — 어떤 점들에 지도를 맞출 것인가
drawDayPins(stops)        일정 탭에만 있는 번호 핀
```

### 왜 합쳤나

`renderMap` · `renderSpotMap` · `renderFoodMap` 셋이 거의 같은 코드를 따로 갖고 있었고,
`curMode` 로 셋 중 하나를 고르는 **똑같은 3갈래 분기가 네 곳**에 흩어져 있었다.
그래서 한쪽만 고쳐지는 일이 실제로 여러 건 있었다.

- `map.stop()` 이 일정 탭에는 아예 없고, 여행지는 레이어 비우기 전, 맛집은 그 뒤에 있었다.
- `pinRefs` 를 일정 탭에서만 채워, 여행지·맛집으로 넘어가도 **지워진 마커 참조가 남았다**.
- 진도 제외 규칙이 맛집에만 있었다(지금은 진도 좌표가 아예 없어 아무것도 안 거르지만,
  앞으로 들어오면 세 탭이 같이 걸러야 한다).
- `initMap()` 보장이 일정 탭에만 있었다.

### 어떻게 확인하나

회귀 점검의 `지도통합` 항목이 `drawMapFor` 를 잠깐 감싸 배율을 하나 바꿔 보고,
세 탭이 모두 따라오는지 본다. 탭별로 되돌리면 `한곳수정이_세탭에` 가 `false` 가 된다.
`핀참조` 항목은 탭을 바꿀 때 `pinRefs` 가 비워지는지 본다.

---

## 13. 일정 편집기

범례 줄 끝의 작은 **`#`** 으로 들어간다. 가족이 받은 링크에는 편집할 이유가 없으므로
화면에 크게 두지 않았다. 실수로 눌러도 `원래대로` 로 완전히 되돌아간다.

설계와 이유는 [docs/superpowers/specs/2026-08-28-schedule-editor-design.md](docs/superpowers/specs/2026-08-28-schedule-editor-design.md),
1단계 구현 계획은 [docs/superpowers/plans/2026-08-28-schedule-editor-phase1.md](docs/superpowers/plans/2026-08-28-schedule-editor-phase1.md).

### 계산 블록은 DOM 을 만지지 않는다

`index.html` 안의 `/* ══════ 일정 계산 ══════ */` ~ `/* ══════ 일정 계산 끝 ══════ */`
사이에는 **순수 함수만 넣는다.** `tools/editor-test.js` 가 이 블록만 꺼내 node 에서
돌리기 때문에, DOM 이나 `localStorage` 를 만지는 순간 시험이 통째로 죽는다.
`localStorage` 를 쓰는 `editLoad` `editSave` `editClear` 와 `fetch` 를 쓰는
`rideFetch` 가 블록 밖에 있는 것은 그래서다.

```bash
node tools/editor-test.js        # 계산 시험 66건 (브라우저 없이)
```

편집 화면 점검은 `tools/editor-check.js` 를 브라우저 콘솔에 붙인다.
**서비스워커가 옛 `index.html` 을 물고 있으면 새 함수가 없다고 나온다** — 확인 전에
워커와 캐시를 지우고 새로 열 것.

### 되돌리지 말 것

- **머무는 시간은 배포판 시각에서 역산한다.** 그래야 편집기를 처음 열었을 때 계산
  결과가 지금 일정과 똑같이 나온다. 값이 갑자기 바뀌면 무엇이 내 편집이고 무엇이
  계산 오차인지 알 수 없다. 5일치 52곳 전부 일치함을 시험이 지킨다.
- **끼니는 배포판 원래 시각으로 정한다.** 계산된 시각으로 정하면 점심을 16시로 밀었을 때
  "저녁이 이르다" 로 경고가 뒤집힌다.
- **읽어낼 수 없는 운영시간·휴무 문구에는 경고하지 않는다.** 잘못된 경고는 없는 것만 못하다.
  `'매월 첫째 월요일'` `'기상 악화 시 통제'` 같은 것은 일부러 안 잡는다.
- **저장은 바뀐 값만.** 정차지 전체를 저장하면 배포판이 바뀌었을 때 옛 값이 되살아난다.
  일정 지문이 다르면 저장을 자동으로 버린다.
- **`render()` 의 `k` 는 지도 핀 번호이지 정차지 번호가 아니다.** 좌표가 없으면 `-1` 이고
  52곳 중 22곳이 그렇다. 편집은 `si`(전체 인덱스)를 쓴다. 이걸 섞으면 휴게소·체크아웃의
  편집이 통째로 어긋난다.

### 편집기가 찾아낸 것 — 그리고 고친 것

경고 규칙을 일정에 대 보니 **2일차(10월 6일 화요일) 14:00 유민미술관이 매주 화요일
휴무**에 걸렸다. 편집기가 없었으면 문 앞에서 알았을 문제다.

**2·3일차를 통째로 맞바꿔 풀었다** (2026-08-28). 동부를 수요일로, 서부를 화요일로 옮겼다.
날짜(`dm`)와 요일(`dw` 앞 글자)은 자리를 지키고 권역과 내용만 옮겼으며 `PATHS` 도 함께 옮겼다.

왜 이 방법인가 — 유민미술관은 **하루 종일** 휴무라 시각 조정으로 못 푼다. 동부를 도는 날은
2일차뿐이라 다른 날에 끼우면 46km 를 돌아가야 한다. 빼는 방법도 있었으나(2일차가 1시간
짧아진다) 맞바꾸면 아무것도 잃지 않는다. 서부는 월·일 휴무만 있어 화요일에 다 열린다.

**딸려온 것 둘.**

- **점심집을 바꿨다.** 백기해녀의집(전복죽)이 수요일 휴무라 그날 못 간다. 성산일출봉에서
  3km 인 **부뚜막식당**(갈치조림)으로 옮겼다. 이 집은 매달 2·4번째 수요일이 휴무인데
  **10/7 은 10월 첫째 수요일이라 연다**(달력으로 계산해 확인).
- **본문의 요일 문구 넷을 고쳤다.** 성산일출봉·김창열미술관·비블리아의 "○요일이라
  문제없음" 과 1일차 끝의 "내일 조식은 09:00부터"(다음 날이 07:30 출발로 바뀌었다).

**부수 효과 — 힘든 날이 앞으로 왔다.** 서부는 190km·12시간, 동부는 90km·10시간이다.
1일차가 배 타고 넘어오는 이동일이므로 그 다음 날이 더 고돼졌다. 체력이 걱정되면
되돌리는 대신 서부의 군산오름 일몰을 포기해 한 시간 당길 수 있다(3일차 저녁 `note` 참고).

이 문제가 다시 생기면 `node tools/editor-test.js` 의 `[지금 일정에 대 보기]` 가 잡는다.
지금은 걸리는 곳이 0 곳이어야 통과한다.

### 장소 바꾸기 — 끝났다 (커밋 `86082c2` · `897b209`)

편집 카드의 `장소 바꾸기` 버튼으로 정차지를 다른 곳으로 교체한다. 여행지 48곳·맛집
51곳에서 고르거나 이름을 직접 적는다(직접 입력은 좌표를 그대로 둔다 — 좌표를 모르면
이동시간을 못 낸다). 맛집은 대표 메뉴를 함께 보여주고, **그날 요일이 정기 휴무인 곳은
흐리게 나오고 고를 수 없다.**

**`stopsOf(day)` 가 통로다.** `D[day].stops` 를 직접 읽지 말고 이걸 쓴다 — 편집을 얹은
사본을 돌려준다. 지금 여덟 곳이 쓴다(`MAPPTS.trip` `focusStop` `editRow` `render`
`dayPlan` `dayWarns` `editCode` `warmRides`). **`planFingerprint` 와 `staticMap` 은 예외로
원본을 쓴다** — 지문은 배포판 기준이어야 저장을 언제 버릴지 판단할 수 있다.

**`dayPlan` 은 머무는 시간을 배포판 좌표·배포판 이동시간에서 역산한다.** 지금 좌표로
역산하면 장소를 멀리 옮겼을 때 이동이 늘어난 만큼 체류가 줄어 **도착 시각이 그대로**가
된다. 멀어진 것은 길이지 머무는 시간이 아니다. 되돌리지 말 것.

---

## 14. 편집을 보기 화면에 반영하기 — 끝났다 (커밋 `5a2e4f6`)

> **여기까지는 `143da60` 으로 원격에 밀려 배포되었다** (2026-08-29).
> 밀 것이 남았는지는 `git log --oneline origin/main..main` 으로 확인한다.
>
> **밀기 전에 회귀 점검을 돌릴 것.** 밀면 3~4분 뒤 배포되어 가족이 본다.
> `9b0fa16`(지도 배경을 카카오맵으로)은 **2026-08-29 에 걷어냈다** — 느려서 국토지리정보원
> 항공사진으로 바꿨다(§5). **항공사진 배경은 아직 실제 휴대폰에서 눈으로 확인하지 못했다.**

편집을 닫아도 바꾼 일정이 그대로 보인다. 여행 중에 비가 오거나 문이 닫혀 그 자리에서
일정을 바꿨을 때, **바뀐 일정을 보면서 다닐 수 있어야 하기 때문이다.** 현장에서는 배포를
할 수 없다. `코드 복사` 는 그대로 두었다 — 확정된 일정을 가족에게 배포할 때 여전히 쓴다.

### 무엇이 달라졌나

**① 저장된 편집은 편집 모드와 무관하게 화면에 반영된다.**
`render` 가 `const 반영=editOn||editHas(i)` 로 가른다. 반영이면 `window.__plan` 에
계산 결과를 담고, 아니면 `null` 로 비운다. 정차지 카드의 시각 칩(`apChip(때)`)과
하루 요약 배너가 이 값을 쓴다. **고친 것이 없는 날은 계산이 배포판과 똑같이 나오므로
아예 건너뛴다** — 그날은 `__plan` 이 `null` 이고 배포판 시각이 그대로 보인다.
지도 핀과 경로는 원래부터 `stopsOf` 를 거치므로 함께 따라온다(확인함).

**② 장소를 바꾸면 옛 곳의 설명과 사진이 떨어진다.**
`stopsOf` 가 `옛설명`(`sub` `feat` `design` `see` `steps` `warn` `note` `img` `imgAlt`
`cap` `bd`)을 지우고 `placeInfo(이름)` 을 얹는다. **`ty` 와 `t` 는 그 자리의 것이라 남긴다.**

- 여행지면 `theme · open` 을 부제로 (예: `숲·생태 · 09:00 ~ 18:00 (입장 마감 17:00)`)
- 맛집이면 `main` 을 부제로, `note` 를 본문으로
- **목록에 없으면 이름만 둔다. 지어내지 않는다.** 직접 입력한 이름이 여기 해당한다.

`cap` 과 `bd` 도 함께 지운다 — 사진 없이 사진 설명만 남거나 옛 곳의 배지가 남으면
바로 그 "이름만 새것" 문제가 된다.

**③ 저장된 편집이 있으면 범례의 `#` 이 진해진다.**
①로 편집이 늘 반영되므로, 부모님이 실수로 눌러 무언가 바꾸면 **되돌릴 입구가 그것뿐이다.**
`.lghash.on{color:var(--sea)}` 와 title 문구만 바꾼다. `hashSync()` 를 `render` 끝에서
부르므로 편집·되돌리기·탭 전환이 모두 자동으로 반영된다.

### 되돌리지 말 것

- **`#` 은 색과 설명만 바꾼다.** 점을 붙이거나 굵게 하면 폭이 달라져 좁은 화면에서
  범례가 두 줄이 된다. 지금 `#` 은 어느 상태에서나 13.84px 다.
- **`__plan` 은 편집이 없는 날 `null` 이어야 한다.** 예전에는 `editOn` 이 아닐 때
  옛 값이 그대로 남아 있었다. 이제 카드 시각이 그걸 읽으므로 반드시 비운다.
- **`__warn` 은 편집 모드에서만 만든다.** 편집 줄에서만 쓰이므로 밖에서는 낭비다.
- **키가 있으면 편집 모드가 아니어도 `warmRides` 를 부른다**(`render` 끝).
  추정값으로 그린 시각이 편집 화면에서 본 것과 달라지면 안 되기 때문이다.
  키가 없으면 받아 봐야 추정값 그대로라 부르지 않는다.

### 확인한 것 (2026-08-29 · 375×667)

1. 3일차 성산일출봉 → 비자림. **편집을 닫은 채로** 10:55 → 11:10 으로 바뀌고 뒤 일정이
   함께 밀린다. 배너 숙소 복귀 19:45 → 20:12.
2. 그 카드에 성산일출봉의 사진·본문이 없다. 부제가 `숲·생태 · 09:00 ~ 18:00` 로 바뀐다.
3. 점심을 범일분식으로 바꾸면 부제 `순대백반`, 본문에 그 집 설명이 붙는다.
4. 목록에 없는 이름으로 바꾸면 `ty` `t` `n` `ll` 만 남는다.
5. 새로고침해도 남는다. `#` 이 `lghash on` · `rgb(21,97,109)` 로 바뀐다.
6. `원래대로` 를 누르면 카드·배너·`#` 이 배포판으로 완전히 돌아간다.
7. 계산 시험 74건 통과. 회귀 점검 — 배지불일치 0 · 핀합계 30 · 경로 5 ·
   핀클릭 간격 8 · 배율 {미술관 14, 일출봉 13, 숲길 12, 맛집 14, 일정_일출봉 13} ·
   탭통일·점클릭·지도통합·점갈래 3종 모두 기대값 · 섬전체 {줌 9, 다보임 true} ·
   전체보기 667 / 닫기 213 · 범례 {높이 29, 한줄 true} · 개략도 58(z9)/77(z12) · 오류 0.

### 회귀 점검에서 걸린 것 — 진짜 회귀가 아니다

`tools/regression-check.js` 의 기대값 몇 개가 낡았다. 다음에 돌릴 때 헷갈리지 않도록 적는다.

- **`지적경계` 는 이제 10 이다.** 다른 세션이 제주현대미술관 지적을 한림읍 월림리
  115-72·115-73 두 필지에서 **한경면 저지리 2114-63** 한 필지로 고쳤다(맞는 주소다).
- **`섬전체` 는 1일차에서 재야 줌 9 다.** 섬 전체 버튼은 `redrawMap()` 을 부를 뿐이라
  **그날 정차지 범위**에 맞춘다. 3일차에서 재면 줌 10 이 나오는데 이건 정상이다.
- **`핀참조.일정` 은 그날 핀 수다.** 1일차 5, 3일차 8. 5 로 못 박을 값이 아니다.
- **`배지불일치` 는 `.sn` 이 아니라 `.pnum` 으로 재야 한다.** `.sn` 은 없는 클래스라
  비교가 통째로 건너뛰어져 늘 0 이 나온다. `.pnum` 으로 재도 0 이다(진짜 통과).
- **`배율.일정_일출봉` 은 `D[1]` 이 아니라 `D[2]` 다.** 2·3일차를 맞바꿨을 때
  성산일출봉이 3일차로 옮겨 갔는데 점검 스크립트가 따라가지 못했다. `D[2]` 로 재면 13 이다.
- **320px 에서 범례는 원래도 두 줄이다**(높이 50). 이 문서가 "375px·320px 둘 다 한 줄"
  이라고 적었던 것은 틀렸다. `9b0fa16` 이전 판으로도 같아서 회귀가 아님을 확인했다.

### 이 저장소는 여러 세션이 동시에 고친다

2026-08-29 작업 중에 다른 세션이 같은 `index.html` 을 고치고 커밋했다(경로 다시 굽기,
공유 장소 기능, 지적 수정). 그래서 **`git add index.html` 로 통째로 커밋하면 남의
진행 중인 작업이 함께 들어간다.** 이 절의 커밋은 그걸 피하려고 이렇게 했다.

```bash
# HEAD 판에 내 수정만 얹은 파일을 만들어 인덱스에만 넣고 커밋한다.
# 작업 트리는 건드리지 않으므로 남의 미커밋 작업이 그대로 남는다.
BLOB=$(git hash-object -w --path=index.html /경로/내수정만.html)
git update-index --cacheinfo 100644,$BLOB,index.html
git diff --cached --stat        # 내 것만 있는지 반드시 눈으로 확인
git commit -F 메시지.txt         # --only 나 -a 를 쓰면 작업 트리가 커밋된다
```

**`git commit --only index.html` 은 인덱스가 아니라 작업 트리를 커밋한다.** 한 번
그렇게 해서 남의 작업까지 커밋했다가 `git reset HEAD~1` 로 되돌렸다.
그리고 **Bash 에서는 PowerShell 의 `@'...'@` 를 쓰면 안 된다** — `@` 가 메시지에 그대로
들어간다. 여러 줄 메시지는 `-F 파일` 로 준다.

---

## 15. 하루 동선은 구간마다 색이 다르다 (커밋 `dd30c10`)

설계는 [docs/superpowers/specs/2026-08-29-route-segment-colors-design.md](docs/superpowers/specs/2026-08-29-route-segment-colors-design.md).

같은 길을 두 번 지나면 선이 한 줄로 포개져 어디로 가는 길이고 어디서 돌아오는 길인지
읽을 수 없었다. **겹친 점의 대부분이 정확히 같은 좌표라**(2일차 70중 42, 4일차 78중 60,
나머지도 평균 7.7~11.5m) 색만 바꿔서는 나중 선이 앞 선을 완전히 덮는다.

### 어떻게 풀었나 (2026-08-29 사용자 요청으로 한 번 바뀌었다)

**차로 가는 길은 실선, 주차한 뒤 걷는 길만 촘촘한 점선(`2 3`)이다.** 선의 모양이 이동
수단을 말한다. 도보선은 주차장과 실제 자리가 **200m 넘게** 떨어진 정차지에만 긋는다 —
글라스하우스 709m · 유민미술관 660m · 수월봉 299m · 성산일출봉 215m · 군산오름 205m
다섯 곳이다. 그 미만은 주차장에서 바로 보이는 거리라 그려도 읽히지 않는다.

**갔다가 같은 길로 돌아오는 구간은 나란히 띄운다.** 60m 안에 든 점이 셋 이상이면 겹친
것으로 보고 한쪽은 왼쪽, 다른 쪽은 오른쪽으로 4px 민다. 4일차 구간3(1100고지→화순곶자왈)과
구간5(송악산→저녁)가 19점 겹치는데 이제 두 선이 나란히 보인다.

**처음에는 점선의 틈 위치를 구간마다 어긋내 풀었다**(겹치면 서로의 틈으로 아래 색이
비친다). 겹침 계산이 필요 없는 장점이 있었으나 선이 전부 점선이라 이동 수단을 모양으로
구분할 수 없어 사용자 요청으로 바꿨다.

### 되돌리지 말 것

- **흰 테두리를 전부 먼저 깔고 색선을 전부 나중에 얹는다**(`paintRoute`). 구간마다
  테두리와 색을 번갈아 그리면 뒤 구간의 흰 테두리가 앞 구간 색을 덮는다.
- **겹친 선을 미는 것은 화면 좌표로 한다**(`offsetSeg`). 좌표를 그대로 밀면 확대할수록
  간격이 벌어져 실제 도로에서 크게 벗어난다. 그래서 배율이 바뀌면 경로만 다시 그린다
  (`redrawRoute` · `zoomend`). **`redrawMap` 을 부르지 말 것** — 점과 핀까지 새로
  만들어 확대할 때마다 화면이 튄다.
- **차량은 실선, 도보만 점선**이다. 전부 점선으로 되돌리면 이동 수단을 모양으로 구분할
  수 없어진다. 도보 기준 200m 도 낮추지 말 것 — 그 미만은 주차장에서 바로 보이는
  거리라 선이 점으로 뭉친다.
- **한 주차장을 여러 정차지가 함께 쓰면 도보선을 하나의 사슬로 잇는다**(`walksOf`).
  주차장에서 각 정차지로 따로 뻗는 방사형으로 그리면 저마다 차를 타고 가는 것처럼
  읽힌다. 3일차가 그렇다 — 휘닉스 아일랜드 주차장 한 곳에 대고 유민미술관 ·
  글라스하우스 · 섭지코지를 걸어서 돈다(사슬 4점 1.41km).
- **선 색의 채도를 낮추지 말 것.** 처음에 올리브·황토가 섞인 색표를 썼더니 지도 배경
  (베이지·연두)에 묻혀 선이 흐릿했다. 지금은 파랑→청록→초록→황금→주황→빨강이고
  불투명도도 1 이다.
- **화살표는 선 안이 아니라 옆에 둔다**(`drawArrows`). 선 안에 흰색으로 넣으면 선을
  갉아먹어 선도 화살표도 흐려진다. 겹쳐 밀린 구간은 제가 밀린 쪽(바깥)으로 더 빼내야
  두 선 사이에 화살표가 끼지 않는다. **색은 선과 같게** 하고 흰 테두리를 둘러야
  어느 동선의 방향인지 한눈에 짝지어진다.
- **화살표 모양은 축에 꺾쇠 촉을 붙인 → 다.** 꽉 찬 삼각형으로도 만들어 봤으나 어느
  쪽이 앞인지 한눈에 들어오지 않는다. 촉은 `stroke-linejoin="miter"` 로 뾰족하게 —
  `round` 면 끝이 뭉쳐 둔해 보인다. 같은 경로를 흰색으로 굵게 한 번, 선 색으로 가늘게
  한 번 겹쳐 그려 테두리를 만든다.
- **화살표 각은 밀리지 않은 원본 좌표에서, 앞뒤로 12px 이상 떨어진 점으로 잰다**
  (`drawArrows`). 이걸 어기면 **특정 배율에서 화살표가 거꾸로 돈다.** 두 가지가 겹쳐
  일어난 일이다 — 단순화된 경로에는 1px 짜리 세그먼트가 섞여 있는데, 그것을 7px 밀면
  두 점의 앞뒤가 뒤바뀐다(실제로 0도가 135도로 튀었다). 밀린 선은 그리기에만 쓰고
  각을 재는 데는 쓰지 말 것.
  **`zIndexOffset` 을 음수로 주지 말 것** — Leaflet 이 화면 y 값에 더해 z-index 를
  만들어서 위쪽 화살표는 최종값이 음수가 되고, 그러면 지도 뒤로 숨어 아예 안 보인다.
  간격은 화면 픽셀로 고정하고 화면 밖은 건너뛴다 — 구간 길이에 비례시키면 확대했을 때
  화살표가 전부 화면 밖으로 나간다.
- **핀은 한 붓으로 그린 SVG 물방울**이다(`drawDayPins`). 원에 삼각형을 덧대면 이음매가
  드러나 어설프다. 뾰족한 끝이 좌표를 가리키므로 `iconAnchor` 를 `[15,40]` 으로 둔다.

- **`DAYCOLOR`(날짜별 색)로 되돌리지 말 것.** 한 화면에는 그날 하루의 선만 그려지므로
  (`drawRoute(curDay, …)`) 날마다 색이 달라 봐야 비교 대상이 없어 아무것도 말해 주지
  않는다. 그 자리를 구간 순서에 내준 것이다.
- **색은 색표에서 균등하게 뽑는다**(`legColors`). 구간이 넷이든 일곱이든 어느 날이든
  청록에서 벽돌까지 같은 흐름으로 읽혀야 한다. 앞에서부터 차례로 쓰면 짧은 날은
  따뜻한 색이 아예 안 나온다.
- **경계를 탐욕적으로 고르지 말 것**(`segsOf`). 정차지마다 가장 가까운 점을 차례로
  집으면, 같은 길을 되돌아 나올 때 뒤에 다시 지나는 자리가 더 가까워 경계가 뒤로
  끌려간다. 3일차 점심집이 실제로 그랬다 — 110번 점(236m) 대신 206번 점(93m)에 붙어
  성산일출봉→점심 구간이 3.9km 대신 **19.2km** 가 되고 섭지코지 일대가 그 구간에
  통째로 들어갔다. 거리 합이 가장 작은 조합을 동적 계획법으로 찾는 지금 방식을 지킬 것.
- **구워 둔 선이 지금 정차지와 1.5km 넘게 어긋나면 나누지 않는다**(`segsOf`).
  편집으로 장소를 옮기면 그 정차지가 옛 선 위에 없어 **경계가 뭉개지고 구간이 통째로
  사라진다** — 3일차 성산일출봉을 비자림으로 바꿔 보니 일곱 구간 중 셋만 그려져 선
  대부분이 화면에서 없어졌다. 그때는 한 줄로 두는 편이 낫다.
  임계값 근거 — 배포판은 가장 먼 정차지도 441m, 비자림으로 바꾸면 7,915m.

### 주차장 좌표를 고쳤으면 경로를 반드시 다시 구울 것 (2026-08-29)

유민미술관·글라스하우스에 주차장 좌표를 넣은 뒤 **경로를 다시 굽기 전까지는 차량 선이
섭지코지 안쪽까지 들어갔다.** `PATHS` 가 건물 좌표로 구워져 있어 건물까지 들어갔다
나오는 길이 자료에 그대로 남아 있었기 때문이다. 코드가 아니라 자료의 문제라 다시
굽는 것 말고는 길이 없다.

```bash
KAKAO_REST_KEY=$(cat tools/.kakao-key) node tools/build-paths.js --dry
KAKAO_REST_KEY=$(cat tools/.kakao-key) node tools/build-paths.js
```

**`tools/.kakao-key` 는 무시 목록에 있다**(`.gitignore`). REST 키는 도메인 제한이 없어
저장소에 올리면 남이 그대로 가져다 쓴다. 파일이 없으면 카카오디벨로퍼스
`제주 여행 가이드`(앱 ID 1560035) 앱 설정 > 플랫폼 키의 **REST API 키**를 넣는다.

**키가 든 명령을 큰따옴표 안에 쓰지 말 것.** `node -e "… $(cat …) …"` 처럼 쓰면 셸이
먼저 값을 펼쳐 **키가 화면과 기록에 그대로 찍힌다.** 2026-08-29 에 실제로 그랬다.
키를 다루는 스크립트는 `Write` 로 파일에 만들어 돌린다.

**지금 키는 한 번 노출된 적이 있다.** 사용자가 재발급하지 않기로 정했다(2026-08-29).
대화 기록 밖으로 나가지는 않았으나, 카카오디벨로퍼스에서 사용량이 튀는지 이따금 보고
이상하면 그때 재발급한다. REST 키는 도메인 제한이 없어 값만 알면 누구나 쓸 수 있다.

다시 구운 결과 — 3일차가 218점 57.81km 에서 **167점 51.31km** 로 줄었다. 빠진 6.5km 가
섭지코지 안쪽 왕복이다. 유민미술관→글라스하우스와 글라스하우스→섭지코지는
`출발지와 도착지가 5 m 이내` 로 실패하는데 **고장이 아니다** — 셋이 같은 주차장을
쓴다는 뜻이고, 그 사이는 도보 사슬이 대신 그린다.

### 어디가 그 일을 하나

```
LEGCOLOR      구간 색표 7단계 (청록 → 벽돌)
legColors(n)  구간 n 개의 색. 색표에서 균등하게 뽑고 모자라면 mixHex 로 섞는다
segsOf(day,pts)  구워 둔 PATHS 를 정차지 사이의 구간으로 나눈다. 거리 합이 가장 작은
                 단조 증가 경계를 동적 계획법으로 찾는다. 정차지 지문으로 캐시
segsPlain(pts)   구워 둔 경로가 없을 때 정차지를 그대로 이은 구간들
walksOf(stops)   주차장과 200m 넘게 떨어진 정차지의 도보선
offsetOf(구간들)  겹치는 구간 쌍을 찾아 밀 방향(+1/-1)을 준다. WeakMap 으로 캐시
offsetSeg(co,px) 화면 좌표로 바꿔 옆으로 밀고 되돌린다
paintRoute(구간들,도보들)  흰 테두리 전부 → 차량 실선 → 도보 점선
redrawRoute()    배율이 바뀔 때 경로만 다시 그린다
```

`PATHS` 자료 구조는 그대로다(하루치가 평평한 좌표 배열). **구간 경계를 앱에서 복원하므로
카카오 키도 다시 굽기도 필요 없다.** 다섯 날 모두 구간 거리 합이 전체 경로 거리와
소수점까지 일치하는 것으로 복원이 맞았음을 확인했다.

개략도(`staticMap`)에도 같은 규칙이 들어 있다.

### 확인한 것 (2026-08-29 · 375×667)

1. 5일치 모두 구간 수 = 정차지 수 − 1, 구간 거리 합 = 전체 경로 거리
   (57.62 · 137.21 · 51.31 · 139.92 · 36.77km, 소수점까지 일치).
   2026-08-29 경로를 다시 구운 뒤의 값이다.
2. 4일차 겹치는 구간을 확대하니 올리브와 벽돌이 번갈아 찍힌 한 줄로 보인다.
3. 겹치는 쌍의 틈 위치가 다르다 — 4일차 14↔5, 3일차 0↔14, 2일차 0↔19.
4. 장소를 바꾸면 한 구간(전체 길이)으로 남고, 되돌리면 다시 나뉜다.
5. 개략도 SVG 도 구간별 색과 어긋난 틈을 쓴다.
6. 회귀 점검 전 항목 · 계산 시험 74건.

**3일차는 빈 구간이 셋이다.** 유민미술관·글라스하우스·섭지코지가 사실상 같은 자리라
(구간 거리 0.0~0.1km) 그릴 선이 없다. 자리는 남겨 두므로 뒤 구간의 색이 밀리지 않는다.

### 확인할 때 걸리는 것

**브라우저 HTTP 캐시.** `tools/serve.js` 가 `Cache-Control: no-cache` 를 주는데도 고친
스크립트가 반영되지 않는 일이 있었다. `?v=아무값` 을 붙여 열면 확실하다.

**`staticMap()` 을 실제 `#map` 에 부르지 말 것.** 확인해 보겠다고 불렀다가 Leaflet 이
관리하는 DOM 이 통째로 갈려 핀이 40개로 세어지는 등 지도 상태가 망가졌다. 회귀로 오인하기
쉽다 — 개략도를 확인할 일이 있으면 페이지를 새로 열고 회귀 점검을 다시 돌린다.

---

## 16. 일정 목록 손보기 — 끝났다 (커밋 `2675be6`)

설계 §8 의 2단계 나머지였다. 이제 편집기에서 **순서 바꾸기 · 넣기 · 빼기**가 된다.

### 무엇이 되나

- **제목 고치기** — 편집 줄 맨 위 `제목` 칸. **장소 바꾸기와는 다른 일이다** — 같은 곳을
  달리 부르는 것뿐이라 설명·사진·좌표를 그대로 둔다(편집 키도 `name` 으로 따로 둔다).
  칸을 비우면 배포판 이름으로 돌아간다.
- **설명 고치기** — `설명` 칸에는 **메모만** 적는다. 거리·시간·요금은 앞에 칩으로 갈라
  놓되 **고칠 수도 지울 수도 없다**(`subParts`). 자동으로 붙는 값이기 때문이다.
  **저장되는 것은 메모뿐이고**, 사실은 `stopsOf` 가 배포판에서 도로 붙인다 — 함께
  저장하면 두 벌이 되어 겹친다. 배포판 메모와 같아지면 편집을 지운다.
  **구분자는 공백을 낀 ` · ` 뿐이다** — 붙여 쓴 가운뎃점까지 나누면 `김밥·샌드위치` 가
  `김밥 · 샌드위치` 로 벌어져 원문이 바뀐다.
- **고친 일정 담아 두기** — 편집 띠의 `저장 1` `저장 2`. 누르면 담기·꺼내기·비우기가
  펼쳐지고 4초 뒤 저절로 접힌다. 한 번에 실행하면 실수로 덮어쓴다. 든 것이 있으면
  단추에 점(●)이 붙는다. **지문을 함께 저장해 배포판이 바뀌면 버린다**(`editLoad` 와
  같은 이유). `원래대로` 를 눌러도 담아 둔 것은 남는다 — 지금 편집을 지우는 것과
  담아 둔 것은 다른 일이다.
- **끌어서 옮기기** — 편집 줄 왼쪽 손잡이(`⠿`)를 잡아 끈다. 화면 끝에 닿으면 목록이
  따라 스크롤된다.
- **위로 · 아래로** — 한 칸씩. 끌기가 어려운 상황을 위한 것이고 결과는 같다.
- **아래에 넣기** — 그 자리 아래에 빈 정차지를 만들고 장소 고르기를 바로 연다.
- **빼기** — 확인을 한 번 묻고 뺀다. 뺀 자리의 편집과 실체도 함께 지운다.

### 어떻게 풀었나 — 편집 키를 순서에서 떼어냈다

**이것이 이 일의 전부다.** 전에는 편집 키가 곧 배포판 인덱스이자 화면 순서였다.
순서를 바꾸거나 중간에 하나를 빼는 순간 그 셋이 어긋난다.

```
editData[일차] = {
  ord:  ['0','1','+1','2', …]   화면 순서. 값은 편집 키다
  신규: { '+1': {n, ll} }        새로 넣은 정차지의 실체
  '0':  {t, stay, fix, place}    정차지별 편집 — 키는 그대로 두었다
}
```

- **키는 이름표다.** 배포판 정차지는 `'0' '1' …`, 새로 넣은 것은 `'+1' '+2' …`.
  키를 그대로 두었으므로 **전에 저장해 둔 편집이 그대로 살아난다.**
- `ord` 가 없으면 배포판 순서 그대로다. 그래서 손대기 전과 완전히 같이 동작한다.
- `stopsOf(day)` 가 각 정차지에 **`_k`(편집 키)** 를 달아 돌려준다. 편집을 찾을 때는
  화면 인덱스가 아니라 이것을 쓴다.

**`_k` 와 자리(화면 순서)를 섞지 말 것.** 편집 값은 `_k` 로 찾고, 계산 결과
(`window.__plan`)는 화면 순서 배열이므로 자리로 찾는다. 이걸 섞으면 순서를 바꾼
순간 남의 값이 보인다.

### 되돌리지 말 것

- **머무는 시간은 편집 키로 역산값을 찾는다**(`dayPlan`). 순서를 바꿔도 그 정차지의
  체류시간이 따라가야 한다 — 머무는 시간은 장소의 속성이지 순서의 속성이 아니다.
  새로 넣은 곳은 배포판에 짝이 없으므로 60분으로 시작한다.
- **배포판 이동시간은 배포판 길이대로 따로 센다.** 넣거나 빼면 두 목록의 길이가
  달라진다. 전에는 `stops.length` 하나로 두 배열을 함께 돌렸다.
- **끌기 판정에 `elementFromPoint` 를 쓰지 말 것.** 지도가 화면 위쪽에 떠 있어 카드를
  가리므로 범례가 잡힌다. 세로 목록이니 **카드의 위아래 범위와 포인터 y 만** 견주면
  무엇이 덮여 있든 옳게 찾는다.
- **`touch-action:none` 은 손잡이에만.** 카드 전체에 걸면 목록을 스크롤할 수 없다.
- **끄는 동안에는 `render` 를 부르지 말 것.** 끌던 카드가 사라져 손이 놓친다.
  DOM 만 옮겨 보여 주고 손을 뗄 때 `ord` 로 확정한다.
- **`confirm`·`prompt` 를 쓰지 말 것**(§9 와 같은 이유). 카톡 인앱 브라우저가 대화창을
  막는데 `null` 을 주는 것이 아니라 **예외를 던져** 그 자리에서 코드가 죽는다.
  빼기는 **두 번 누르게** 했고(처음 누르면 `정말 뺄까요?` 로 바뀌고 4초 뒤 되돌아간다),
  장소 직접 입력은 **찾기 칸에 친 글자를 그대로 이름으로 쓴다**(칸이 비었으면 안내만 띄운다).
  처음에 빼기를 `confirm` 으로 만들었다가 이 함정에 그대로 걸렸다 — 인수인계에 적혀
  있는데도 그랬다.

### 코드 복사는 어떻게 나오나

순서를 바꾸거나 넣고 뺀 날은 조각만으로 옮길 수 없다. 그런 날은 **어떤 순서로
재배열할지를 먼저 적고** 그다음 값을 낸다. 사진(base64)이 들어 있어 `stops` 를 통째로
출력할 수는 없기 때문이다.

```
/* 3일차 — stops 를 이 순서로 다시 늘어놓으세요 */
/*   0번 펜션 조식  →  1번 숙소 출발  →  3번 성산일출봉  →  … */
/*   [뺌] 2번 광치기해변 */
/* 3일차 펜션 조식 */ t:'09:00'
```

**옮긴 뒤에는 경로를 반드시 다시 굽는다** — `KAKAO_REST_KEY=$(cat tools/.kakao-key) node tools/build-paths.js`.
안 그러면 배포판의 선만 옛 순서로 남는다.

### 확인한 것 (2026-08-29 · 375×667)

1. 순서를 바꾸면 시각이 다시 계산된다 — 유민미술관을 점심 앞으로 올리니
   12:42 유민 · 13:51 점심으로 바뀌었고, 되돌리니 원래 시각으로 돌아왔다.
2. 넣기 — `ord` 에 `'+1'` 이 끼고 정차지가 10곳에서 11곳이 되며 장소 고르기가 바로 열린다.
   비자림을 고르니 이름·좌표가 들어가고 뒤 일정이 밀렸다.
3. 빼기 — 새 정차지를 빼면 `신규` 까지 지워지고, 배포판 정차지(광치기해변)를 빼면
   `ord` 가 `0,1,3,…` 이 된다. 지도 핀도 9 → 8 → 7 로 따라온다.
4. 끌기 — 손잡이를 잡아 5번째 카드를 3번째 자리로 끄니 `ord` 가 `0,1,5,2,3,4,…` 가 되고
   시각이 다시 계산된다.
5. `원래대로` 를 누르면 편집이 0 이 되고 정차지 10곳·핀 8개로 완전히 돌아온다.
6. 회귀 점검 전 항목 · 계산 시험 74건.

### 그 뒤에 남는 것

설계 §8 의 1·2단계가 모두 끝났다. 지금 남은 것은 기능이 아니라 **자료**다 —
§8 의 "자료 구멍" 과 맛집 영업시간(`FOOD.hours` 는 51곳 중 4곳뿐)이다.

---

## 17. 전체 검증과 그 결과 (2026-08-30)

"사용상 오류가 없는지 불편한게 없는지 전체 검증" 요청으로 여섯 관점 다중 에이전트
감사를 돌리고, 살아남은 24건을 **전부 브라우저에서 직접 재현해 확인한 뒤** 고쳤다.
재현하지 않은 지적은 하나도 채택하지 않았다.

### 고친 것 — 다시 깨뜨리지 말 것

**타일 레이어에 `redraw()` 금지** — §5 에 따로 적었다. 폰을 돌리면 배경이 통째로 사라졌다.

**하루 요약 배너는 편집 키로 찾는다.** 배포판 자리(인덱스)로 편집 결과를 읽으면
정차지를 빼는 순간 남의 시각을 가져오고 범위를 넘으면 `NaN:NaN` 이 찍힌다.
짝이 없는 배너 항목(숙소 복귀 등)은 **가장 가까운 남은 정차지**가 밀린 만큼 민다 —
앞의 마지막 정차지만 보면 그 사이를 통째로 빼 버렸을 때 기준을 잃는다.

**`recalc` 은 못 박기 전 도착(`natural`)을 함께 낸다.** 이걸 지우면 시각을 못 박는
순간 `arrive === pin` 이 되어 `fixWarn` 이 영원히 `null` 을 낸다 — 배를 놓치는데
앱이 아무 말도 안 하게 된다. 경고는 반드시 `p.natural[i]` 로 견줄 것.
첫 정차지(i===0)는 앞 일정이 없어 견줄 것이 없으므로 판정에서 뺀다.

**하루의 시작은 `배포[0].t` 가 아니다.** 목록에 남아 있는 배포판 정차지 중 배포
순번이 가장 앞선 것의 시각이다. 화면 맨 위에서 읽으면 순서만 바꿔도 하루가 옮겨지고,
`배포[0]` 을 그냥 쓰면 그 정차지를 뺐을 때 없어진 시각으로 하루가 앞당겨진다.

**이동 사슬은 좌표 없는 *새* 정차지만 건너뛴다.** 배포판의 좌표 없는 정차지 22곳까지
함께 이으면 `fillStay` 가 이미 stay 로 흡수한 주행을 두 번 세게 되어 배포판 시각이
어긋난다(5일차 체크아웃, 3일차 펜션 조식에서 clamp 된다).

**식사 경고의 끼니는 이름에서 읽는다.** 시각 경계(11:00/15:00)와 창의 경계가 어긋나
09:30~11:00 은 늘 "아침이 늦다", 11:00~11:30 은 늘 "점심이 이르다" 가 됐다.
이름에 끼니말이 없으면 **배포판 시각에 가장 가까운 창**으로 정한다 — 판정에서 빼면
장소를 바꾼 정차지가 아무리 밀려도 영영 조용해진다.

**`stopsOf` 의 사실 조각은 `바탕`(옛 곳)이 아니라 새 곳에서 뽑는다.** 그리고 저장된
메모에서 자동 부제 조각을 떼어낸다(`자동빼기`) — 안 그러면 이미 저장된 편집에서
같은 조각이 두 벌로 나온다.

**서비스워커는 살아 있는 서버를 캐시하면 안 된다** (`sw.js`). 공유 목록(duckdns)은 늘
같은 주소 `?rev=N` 을 부르므로 한 번 캐시에 들어가면 스스로 풀리지 않는다 —
남이 넣은 맛집이 영영 안 보인다. `cache:'no-store'` 로는 워커를 못 막는다.
길찾기(kakaomobility)·장소검색(dapi.kakao)도 함께 제외했다.
제외 목록을 고칠 때는 **CACHE 이름도 올려야** 이미 굳은 응답이 지워진다.

**`setBuild('ok')` 에서 배율 상한을 반드시 되돌린다.** 개략도로 내려가며 12 를 걸고
안 풀면, 지도는 멀쩡해 보이는데 맛집을 눌러도 확대되지 않고 `.build.ok{display:none}`
때문에 다시 시도 단추도 사라져 풀 길이 없다.

**`loadKakaoSdk` 는 실패한 약속을 기억하지 않는다.** 쿼터 초과·도메인 미등록이면
카카오가 200 으로 오류 본문을 주므로 `onerror` 가 안 난다. 시간 제한(12초)과
`kakaoSdk=null` 이 없으면 한 번 매달린 뒤로는 새로고침 전까지 검색이 죽는다.

### 자료를 고친 근거

- 준비물 탭·성산일출봉 상호 참조 다섯 곳이 2·3일차 맞바꾸기 뒤 하루 틀린 채 남아 있었다.
- 운영시간 세 곳은 visitjeju.net 을 근거로 두 탭을 맞췄다. 유민미술관 공식 안내는
  "비정기 휴무(홈페이지 공지)" 라 `off` 에 그 사실을 적되 `매주 화요일` 표현은 남겼다 —
  `offDay()` 가 그 표현만 읽으므로 지우면 화요일 경고가 죽는다.
- 사려니숲길 1:20→1:00, 신창풍차해안 1시간→50분. 도로 시간은 카카오 실측을 따르기로
  했으므로 어긋나면 양보하는 쪽은 머무는 시간이다. **일정 시각은 안 건드렸다** —
  늘리려면 그날 뒤 일정을 다시 짜고 경로를 다시 구워야 한다.
- 송악산을 `SPOT` 에 넣었다(49곳). 내용은 일정 정차지에서 옮겼고 주소는 `AREAS` 의
  지적 표기를 따랐다. 회귀 점검의 `점갈래` 기대값이 48 → 49 로 바뀌었다.

### 회귀 점검 기대값이 바뀐 것

`범례 {높이 29, 한줄 true}` → **`{높이 71±2, 한줄 false}`**. 지도 조작 단추를 44px 로
키워 범례와 한 줄에 안 들어간다. 29 로 돌아가면 단추가 다시 작아진 것이다.
`섬전체` 기대값은 **1일차 기준**이다 — 3일차는 동부만 도는 날이라 줌 10 이 정상이다.
`tools/regression-check.js` 가 성산일출봉을 `D[1]` 에서 찾고 있던 것도 `D[2]` 로 고쳤다.

### 숙소 좌표를 넣었다 (2026-08-30)

4일차 `출발 → 17:25 숙소` 는 좌표가 없어 송악산→숙소 구간이 계산에서 통째로 빠져
있었다. 숙소(**티파니에서아침을**, 남원읍 서의로154번길 12)는 카카오 장소검색으로
`[33.304996, 126.712176]` — 1·3·5일차에 이미 든 `[33.3050,126.7122]` 와 같은 곳이다.

카카오 예측출발(10/8 16:40)로 재니 **75분 56km** 다. 적혀 있던 45분보다 30분 길다.
좌표가 생기면 `t` 는 도착 시각으로 해석되므로 17:25 를 그대로 두면 `fillStay` 가
송악산 체류를 80분 → 5분으로 깎는다. 그래서 시각을 함께 옮겼다.

**그 뒤 순서를 바꿨다 — 저녁을 먼저 먹고 숙소로 간다 (같은 날).** 숙소를 먼저 들르면
위미리를 지나쳐 동쪽으로 갔다가 되돌아오게 되어, 숙소에 머물 수 있는 시간이 18분뿐이었다.

```
[바꾸기 전] 송악산 75분 → 숙소 17:55 → 18분 → 17분 → 저녁 18:30 → 14분 → 귀가 19:44
            주행 106분 75.5km
[지금]      송악산 69분 → 저녁 17:50 → 60분 → 16분 → 숙소 19:05
            주행 85분 58.1km   (21분 · 17.4km 줄고 귀가가 39분 빠르다)
```

15:20 송악산 도착 → 81분(적어 둔 1:20) → 16:40 출발 → 69분 → 17:50 저녁 → 16분 → 19:05 숙소.
저녁이 17:50 이라 이른 편이지만 저녁 창(17:30~20:00) 안이라 경고는 안 뜬다.
송악산에서 일몰까지 보고 17:00 에 나서면 저녁 18:10, 귀가 19:25 다(송악산 warn 에 적어 두었다).

`--dry` 로 재 보면 실측이 지금 일정 안에 다 들어간다(밀리는 구간 0). 4일차 하루 주행은
실측 합계 156.6km 라 배너를 `약 155km` 로 고쳤다.

**정차지에 좌표를 넣거나 뺄 때는 반드시 `t` 가 무엇을 뜻하는지 먼저 본다.** 좌표가
없는 정차지의 `t` 는 사람이 손으로 적은 값(여기서는 *출발* 시각)일 수 있는데,
좌표가 생기는 순간 계산이 그것을 *도착* 시각으로 읽는다.

### 4일차는 송악산에서 일몰을 본다 (2026-08-30)

**송악산 일몰 10/8 18:13** (NOAA 태양위치 계산. 서울 하지 19:58·동지 17:18·춘분 18:44 로
검산해 알려진 값과 1~4분 안에 든다. 계산기는 남겨 두지 않았으니 필요하면 다시 짜라 —
`h0=-0.833°`(대기굴절), 율리우스일은 정오 기준이라 시간대만 얹고 `+0.5` 를 하면 안 된다.)

둘레길 1시간 20분을 **16:55** 에 시작하면 전망대에서 해가 진다. 그러면 화순곶자왈(15:00
출발)과 송악산 사이가 1시간 반 비는데, 직행이 20분뿐이라 그 자리에 카페를 넣었다.

```
13:40 화순곶자왈 → 15:20 카페 감저 → 16:55 송악산 → 18:13 일몰 → 19:35 저녁 → 20:50 숙소
```

**카페는 감저**(대정읍 대한로 22). 곶자왈 18분·송악산 10분이라 직행보다 8분만 더 돈다.
앱 자료가 ★강추이고 "층고가 높고 좌석 간격이 넓어 어르신이 다니기 편하다"고 적힌 곳이다.
visitjeju.net 확인: **라스트오더 18:00 · 매주 월요일 휴무** → 10/8 목요일이라 문제없다.
가까운 대안은 커피스케치(사계리, 곶자왈 12분·송악산 10분 — 직행보다 2분)인데 영업시간을
확인하지 않았다.

**대가는 늦은 귀가다.** 저녁 19:35 · 숙소 20:50 이고, 일몰 뒤 송악산→위미리 67분이
야간 주행이다. 서두르려면 17:30 에 나서면 된다(송악산 warn 에 적어 두었다).

### ⚠ 앱의 모든 이동시간은 카카오맵 실측이다 (2026-08-30 · 사용자 지시)

**손으로 어림한 시간을 넣지 말 것.** 정차지를 옮기거나 좌표를 넣은 뒤에는
`tools/build-rides.js --dry` 로 재고, sub·note·배너의 글까지 그 값으로 고친다.
가짜 시간을 넣으면 실제로 다녀 보았을 때 일정이 엉망이 된다는 것이 사용자의 뜻이다.

2026-08-30 에 앱 전체를 대 보고 고친 값 — 얼마나 어긋나 있었는지 남겨 둔다:

| 구간 | 적혀 있던 것 | 실측 |
| --- | --- | --- |
| 1일차 음성 대소 → 진도항 | 385.4km · 4:45 · 13,800원 | 382.8km · **4:22** · **15,000원** |
| 1·2일차 숙소 → 저녁(위미리) | 15분 | **17분** |
| 2일차 숙소 → 김창열미술관 | 1:15 | **1:07** |
| 2일차 군산오름 → 저녁(위미리) | 45분 | **59분** ← 14분 모자랐다 |
| 2일차 저녁 → 숙소 | 15분 | **16분** |
| 3일차 숙소 → 섭지코지 주차장 | 40분 | **49분** |
| 3일차 종달리 → 숙소 | 45분 | **58분** ← 13분 모자랐다 |
| 4일차 숙소 → 사려니숲길 | 25분 | **16분** |
| 5일차 숙소 → 제주항 2부두 | 55분 | **50분** |
| 5일차 진도항 → 음성 대소 | 385km · 약 5시간 | 386.2km · **4:28** · 15,000원 |

시각까지 옮겨야 했던 곳은 2일차뿐이다 — 군산오름에서 저녁까지 59분인데 50분만 잡혀
있었다. 저녁 19:20 → 19:30, 숙소 도착 20:30 → 20:45.
3일차 배너의 숙소 복귀도 19:45 → 19:58 로(종달리 19:00 출발 + 58분), 준비물 탭의
"야간 중산간 45분" 도 58분으로 고쳤다.

**5일차 귀갓길은 여전히 빠듯하다.** 진도항 14:20 → 음성 18:50 이 4시간 30분인데
주행만 4시간 28분이다. 저녁을 제대로 앉아서 먹으면 그만큼 늦어진다 — 그래서 정차지
이름이 `저녁 · 휴게소 또는 도착 후` 다. 시각은 사용자 판단이라 손대지 않았다.

### 맛집 휴무는 hours 문구 안에 있다 (2026-08-30)

**`FOOD` 에는 `off` 칸이 없다.** 여행지(`SPOT`)는 `open`·`off` 로 나뉘어 있지만 맛집은
`hours` 한 칸에 영업시간과 휴무가 함께 들어 있다 — `09:30–20:00 · 브레이크 15:30–17:00 · 목 휴무`.
자료를 바꾸지 않고 읽는 쪽을 맞췄다. 네 군데가 함께 막고 있었다:

- **`offDay`** 가 `매주 X요일` 만 읽었다. 이제 한 글자 요일과 `정기` 도 읽는다.
  **`매달 2·4번째 수요일` 같은 격주·월간은 그대로 안 잡는다** — 매주가 아닌데 그날 무조건
  쉰다고 하면 갈 수 있는 날을 막는다. 가드는 `매월|매달|첫째|둘째|셋째|넷째|격주|N·M번째`.
- **`openHours`** 가 물결표·하이픈만 받아 맛집의 en dash(`–`)를 못 읽었다. 그리고
  **`상시 개방` 안내문의 괄호 속 시간을 폐장 시각으로 읽어 헛경고를 냈다** — 종달리
  `숍들은 대개 11:00~17:00`, 쇠소깍 `배 체험 09:00~18:00`, 방주교회 `내부 관람 10:00~16:00`.
  그 시간은 딸린 시설 것이고 장소 자체는 늘 열려 있다. 이제 `상시 개방`·`제한 없음` 이
  들어 있으면 시각을 안 잡는다(26곳).
- **`pickList`** 가 맛집 줄에 `막힘:''` 을 무조건 넣었다. 이제 `offDay(x.hours)` 로 판정한다.
- **`dayWarns`** 조회표가 `SPOT` 만으로 만들어져 맛집 이름은 검사에서 빠졌다.
  맛집을 `{n, off:hours, open:hours}` 꼴로 맞춰 함께 넣는다 — `spotWarn` 은 그대로 쓴다.

결과: 휴무가 잡히는 맛집이 0 → 32곳. 4일차(목) 장소 바꾸기에서 야원·성읍칠십리식당이
`그날(목요일) 휴무` 로 막힌다. **지금 일정에 새로 걸리는 경고는 없다**(헛경고 0).

남은 한계 — 비블리아의 `매주 일요일 오전 (종교 행사)` 는 오전만 닫는데 `offDay` 가
`일` 을 내어 일요일 온종일 막는다. 이번 여행에는 일요일이 없어 손대지 않았다.
반나절 휴무를 담으려면 자료 모양부터 바꿔야 한다.

### 지도 옅은 점은 이름이 아니라 거리로 겹침을 본다 (2026-08-30)

`drawPoi` 가 `f.n.startsWith(q[0])` 로 "맛집 이름이 지역 라벨로 시작하면 같은 곳" 이라고
보아 옅은 점을 건너뛰었다. 그래서 `한림칼국수` 가 지역 라벨 **한림**(627m 떨어짐)을,
`위미중앙정육식당` 이 **위미**(215m)를 지도에서 지웠다. 이제 60m 안일 때만 건너뛴다.
실제로 같은 곳인 넷(목화휴게소 3m·동백포레스트 1m·테라로사 21m·서귀다원 19m)은 그대로 걸러진다.

### 카카오(다음) 검색으로 자료를 채운다 (2026-08-30 · 사용자 지시)

같은 REST 키로 다음 검색이 된다 — `https://dapi.kakao.com/v2/search/web`,
`/v2/search/blog`. 맛집 영업시간을 9 → 51/59곳으로 이렇게 채웠다.

**거르는 규칙이 핵심이다.** 그냥 "가게이름 지역 영업시간" 으로 찾으면 그 동네의 다른 집이
딸려 나온다(표선을 찾으면 광어다·표선수산마트가 나왔다). 그래서:

1. 글에 **가게 이름**이 있고 **그 가게 주소 조각**(도로명+번지 또는 리·동)도 함께 있어야 본다.
2. 영업시간·휴무·브레이크·라스트오더 꼴을 뽑아 **같은 값이 몇 번 나왔는지 센다.**
3. **여러 글에서 겹치는 값만 넣는다.** 한 번뿐이거나 엇갈리면 비워 둔다 —
   틀린 시간을 적으면 헛걸음한다.

전화번호도 같은 방법으로 유선 대안을 찾아봤는데 믿을 만한 것은 **산방산초가집** 하나뿐이었다
(안심번호 `0507-1442-0688` 의 뒷자리가 유선 `064-792-0688` 과 일치). 나머지는 단서가
하나뿐이고 뒷자리도 달라 넣지 않았다. 사라질 수 있는 번호는 **16곳**이다
(0507 11 · 010 4 · 070 1) — §8 의 "10곳" 은 맛집이 51 → 59곳으로 늘기 전 숫자다.

### 미리보기 문구는 화면에 안 보여서 조용히 낡는다 (2026-08-30)

`og:description` 과 `meta description` 의 "여행지 N곳 · 맛집 M곳" 은 카톡으로 보냈을 때만
보인다. 여행지·맛집을 늘려도 아무도 눈치채지 못해 48곳·51곳으로 낡아 있었다.
`tools/editor-test.js` 에 **배열 길이와 대 보는 검사**를 넣었다 — 이제 늘리면 시험이 깨진다.
실제로 종달리 식당 셋을 넣자마자 이 검사가 잡아냈다.

### 그 밖에 고친 것 (2026-08-30)

- **낡은 문장 셋** — 송악산의 "오전에 본 수월봉"(이틀 전 오후다), 종달리의 "아침에 코앞에서
  본 산"(두 시간 전이다), 1일차 숙소 체크인의 "내일은 07:30 에 나섭니다"(08:45·09:45다).
  마지막 것은 첫날 밤 알람을 한 시간 넘게 일찍 맞추게 만드는 자리였다.
- **제주항 좌표** — 1·5일차 정차지가 `[33.5169,126.5316]`(건입동 984-22)이었는데 부제가
  적어 둔 제주연안여객터미널(임항로 111)은 `[33.5196,126.5350]` 으로 413m 떨어져 있었다.
  **정차지 좌표는 `tools/coord-audit.tsv`(여행지·맛집 99건) 대상 밖이라 검증된 적이 없다** —
  숙소·식사 동네 핀도 마찬가지다.
- **배지와 목록이 어긋난 둘** — 비블리아(`일 휴무` → `일요일 오전 휴무`),
  글라스하우스(`운영시간 확인` → `10:00–21:00 (매장별 상이)`).
- **부뚜막식당** 격주 휴무가 일정 note 에만 있고 맛집 카드에 없었다.
- **2일차 머리말**만 `편도 1시간 15분`(일정 칸 길이)을 썼다. 실측 1시간 7분으로 맞췄다.
- **종달리 저녁 자리 반경 500m 에 앱이 아는 식당이 하나도 없었다**(가장 가까운 것이 2.16km).
  카카오 반경 검색으로 어멍이해녀(42m)·해산물한접시(111m)·소금바치순이네(483m)를 넣었다.
  셋 다 3일차 수요일에 연다.

### ⚠ 육지 좌표를 정차지에 넣지 말 것 — 경로선이 완도로 돌아간다 (2026-08-30)

1·5일차 점심(진도)에 좌표를 넣었더니 `build-paths.js` 가 **진도→제주를 도로로** 물었고,
육로가 없으니 카카오가 완도 쪽 해안도로를 타고 돌아가는 선을 돌려주었다.
1·5일차 경로선에 위도 34.6 까지 올라간 점이 255~259개 들어갔다.

**되돌렸다** — 두 점심의 `ll` 을 뺐다. 지금 1·5일차 선은 제주 안에서만 그려진다
(위도 34 넘는 점 0개).

**안전장치를 달았다.** `build-paths.js` 는 이제 좌표 있는 두 정차지 사이에 `ship`
정차지가 있으면 그 구간을 잇지 않는다. 판정을 확인했다 —
1일차 `점심(진도) → 제주항 하선`, 5일차 `제주항 2부두 → 점심(진도)` 딱 둘만 걸린다.

**그래도 육지 좌표를 넣지 마라.** 선은 안전장치가 막지만 `segsOf` 가 따로 막힌다 —
구워 둔 선에서 1.5km 넘게 떨어진 정차지가 있으면 하루를 통째로 한 구간으로 돌려주어,
그날 경로선이 단색이 되고 `pinColors` 도 짝을 못 맞춰 핀 색이 사라진다.
진도 핀은 어차피 `drawMapFor` 의 `p[0]<33.7` 걸러내기 때문에 섬 기본 화면에도 안 나온다.
얻을 것이 없다.

### 식사 정차지에도 번호 핀을 찍는다 (2026-08-30)

가게를 정해 둔 곳은 그 가게, 아직 안 정한 곳은 **식당이 모여 있는 동네**를 찍는다 —
거기서 골라 보시라는 뜻이다(사용자 지시).

```
1·5일차 점심 · 진도 복탕·백반  [34.3707,126.2009]  굴포식당 (CAND 에 있는 그 집)
2일차   점심 · 저지·한림       [33.4149,126.2613]  한림 식당가 (보말칼국수·해물류)
3일차   저녁 · 종달리·세화     [33.4995,126.9111]  해맞이해안로 식당가
4일차   점심 · 화순            [33.2504,126.3388]  알동네집·명경식당 가운데
```

**안 찍은 것** — 1일차 07:10·09:40 휴게소, 1일차 14:00 선내 점심, 5일차 17:30 휴게소.
고속도로 휴게소와 배 안이라 맛집을 찾아볼 동네가 아니다.

**1·5일차 점심 핀은 섬 화면 밖에 있다.** `drawMapFor` 가 `p[0]<33.7` 로 걸러 제주만
맞추기 때문이다(진도가 섞이면 제주가 작아진다). 목록에는 번호가 붙고 카드를 누르면
지도가 굴포식당으로 날아간다 — 확인했다. 기본 화면에만 안 보인다.

좌표가 생기면서 그 앞뒤 구간이 실측으로 잡혀 체류가 다시 계산된다. 4일차 점심에
좌표를 넣자 1100고지 체류가 40분 → 33분으로 줄어, 점심을 12:30 → 12:40 으로 미뤄
43분으로 되돌렸다. **좌표를 넣은 뒤에는 체류가 라벨과 맞는지 반드시 다시 본다.**

### 일몰 자리 — 군산오름으로 정했다 (2026-08-30)

2일차에 신창풍차해안과 군산오름이 둘 다 일몰 명소다(10/6 일몰 18:16, 두 곳 같다).
**군산오름을 일몰로, 신창풍차해안은 낮에** 보기로 정했다(사용자 판단).
신창에서 일몰을 보면 저녁까지 76분 55km 라 귀가가 21:07 이 된다 —
군산오름은 59분이라 19:30 저녁·20:45 귀가다.
지금 일정이 이미 그 모양이다: 신창풍차 14:50, 군산오름 17:10 도착 · 18:30 하산.

### 섭지코지 묶음의 걷는 시간은 실측이다 (2026-08-30)

카카오 도보 길찾기로 잰 값이다. 직선거리 어림은 실제 길과 다르다(유민→글라스하우스는
직선 149m 인데 길로는 242m).

```
주차장 → 유민미술관      862m · 14분
유민미술관 → 글라스하우스 242m ·  4분
글라스하우스 → 방두포등대 577m · 10분
등대 → 주차장            661m · 11분   = 한 바퀴 2.3km · 39분
(해안 산책길을 더 돌면 3.3km · 57분)
```

정차지에 **`walk`**(다음 정차지까지 걸어서 몇 분)로 적어 두었고 `hopRow` 가 그것을 먼저 쓴다.
여행 전체에서 걸어서 옮기는 구간은 이 둘뿐이라 표를 따로 만들지 않았다.

묶음 안의 시간도 다시 나눴다 — 글라스하우스 30분 → 50분(걷기 10분을 빼면 카페 40분),
섭지코지 72분 → 61분(산책 50분 + 주차장 복귀 11분). 묶음 전체 길이는 그대로다.

### 3일차는 섭지코지를 오전에 본다 (2026-08-30)

오후에는 휘닉스 C구역 주차장이 찬다는 판단으로 순서를 바꿨다. 덤으로 지그재그가 없어져
남(숙소)에서 북(종달리)으로 한 방향이 된다.

```
09:45 숙소 출발 → 49분 → 10:35 유민 → 11:35 글라스 → 12:25 섭지코지
  → 9분 → 13:35 점심(부뚜막) → 3분 → 14:50 광치기 → 4분 → 15:15 일출봉 → 11분 → 17:00 종달리
하루 주행 84.5km (종달리→숙소 35.5km 포함)
```

**종달리 17:00 은 지인분과의 약속이라 옮기지 않는다.** 성산일출봉 왕복 1:30 이 끝나면
16:49 이고 11분 달리면 17:00 이다 — 여유가 거의 없으니 앞을 늘리면 여기가 먼저 깨진다.

**광치기해변이 오전에서 오후로 갔다.** 밀물이면 암반이 잠겨 볼 것이 없어지는 곳이라
경고를 붙였다 — 성산포 기준 간조 시각을 확인하라는 내용이다.
2026년 물때는 공개 자료로 확인이 안 돼 값을 적지 못했다.

순서를 바꾸면서 낡아진 문구도 고쳤다: 점심 부제(성산일출봉에서 → 섭지코지에서),
점심 note 의 셔틀 안내, 일출봉 see 의 "아침에 광치기에서", 일출봉 매표 마감 문구.

### 2일차 미술관은 하나만 간다 (2026-08-30)

저지문화예술인마을의 두 곳이 겹쳐 **제주현대미술관을 뺐다.** 앱 자료가 스스로 그쪽을
"관심 없으면 생략 가능", "운영시간·휴관일·관람료를 확인하지 못했습니다",
"생략해도 그날 일정에 영향 없음" 으로 적어 두었기 때문이다.
여행지 탭(SPOT)에는 그대로 남아 있어 나중에 바꿔 넣을 수 있다.

빈 1시간 15분은 **늦은 출발**로 돌렸다 — 가장 멀리 나가는 날(편도 1:15, 20:30 복귀)이라
아침잠이 가장 값지다. 아침 07:30→08:45, 숙소 출발 08:30→09:45, 김창열 09:45→11:00.
**김창열 뒤의 시각은 하나도 안 건드렸다** — 점심 12:15 부터 그대로다.

### 카드 사이에 이동 수단과 시간을 적는다 (2026-08-30)

`hopRow(목록,i,이동)` 가 카드 뒤에 `차로 N분 이동` / `도보로 N분 이동` 을 붙인다.
편집 중에는 `.eride` 가 같은 자리를 쓰므로 붙이지 않는다.

- **같은 주차장(5m 안)을 쓰는 이웃은 걷는다.** 걷는 빠르기는 `도보속도=3.5km/h` —
  부모님 걸음이라 평지 기준 4km/h 보다 낮춰 잡았다.
- **글의 도보 기준(50m)은 지도의 것(`도보최소` 200m)보다 낮다.** 200m 아래는 선으로
  그리면 안 보이지만 글로는 알려 줄 값이 있다(유민미술관→글라스하우스 149m).
- **좌표 없는 정차지의 앞뒤로는 아무것도 적지 않는다.** 그 구간의 주행은 배포판 시각
  차이가 체류에 흡수하고 있어 얼마가 이동이고 얼마가 머무는 시간인지 가릴 수 없다.
  4일차에서는 아침↔숙소 출발, 숙소 출발→사려니, 1100고지→점심, 점심→곶자왈 네 곳이
  비어 있다. 채우려면 `숙소 출발` 에 숙소 좌표를, `점심 · 화순…` 에 식당 좌표를 넣어야
  하는데, 그러면 그 정차지들의 `t` 가 도착 시각으로 해석되어 체류가 다시 나뉜다.

`render` 는 이제 편집 여부와 상관없이 `dayPlan(i)` 을 늘 계산한다(`계획`). `window.__plan`
은 여전히 편집이 반영될 때만 둔다 — 그것으로 배너와 카드 시각이 갈린다.

`tools/editor-test.js` 의 "52곳" 단정은 없앴다 — 정차지를 넣을 때마다 깨지므로
어긋난 개수만 본다.

### 핀 색은 그곳에 닿는 구간의 색이다 (2026-08-30)

선은 구간마다 색이 다른데 핀은 정차지 갈래(식사·배·여행지) 색이라, 3일차 동부처럼
선이 교차하는 곳에서 어느 선이 어느 곳으로 가는지 알 수 없었다.

`pinColors(day, 점들)` 이 `[null, 구간0색, 구간1색, …]` 을 낸다 — 핀 `i` 는 구간 `i-1`,
즉 **그곳에 닿는** 구간의 색이다. 첫 정차지는 닿는 구간이 없어 `null` 이고 갈래 색이
그대로 남는다. 색은 인라인 `--pinfill` 로 얹고 CSS 가 `var(--pinfill, 갈래색)` 으로 받는다
— `.pin.meal` 쪽 특이도가 더 높아서 기본 규칙만 고치면 식사 핀에 안 먹는다.

**구간 수가 정차지보다 하나 적지 않으면 색을 주지 않는다.** 구워 둔 선이 지금 정차지와
안 맞아 `segsOf` 가 통째로 한 구간을 낸 경우인데, 짝을 못 맞추면서 색만 칠하면
틀린 곳을 가리킨다.

목록의 번호 배지(`.pnum`)도 같은 색을 쓴다 — 둘은 같은 번호·같은 색이라는 것이
원래 규칙이고, 한쪽만 바꾸면 목록과 지도가 다른 색으로 같은 곳을 가리킨다.

회귀 점검의 **핀합계가 30 → 31** 이다(4일차 숙소).

### 남은 것 (고르지 않았거나 범위 밖)

- **`warmRides` 는 이웃 쌍만 실측을 받아 온다.** 새 정차지를 사이에 두고 떨어진 두
  좌표는 그 고리에서 안 받아 오므로 구워 둔 `RIDE` 나 추정식으로 떨어진다.
  배포판에서 붙어 있던 쌍은 실측이 살아 있어 실제로는 잘 안 드러난다.
- **송악산→숙소 45분**(4일차 `출발 → 17:25 숙소`)은 좌표가 없어 계산에서 통째로 빠진다.
  구워 둔 경로로 재면 그 구간이 48km 급이라 45분은 20~30분 낙관적이다.
  고치려면 숙소 좌표를 정차지에 넣고 경로를 다시 구워야 한다.
- **1일차 첫 정차지를 빼면 배너 첫 칸이 `5:40 대소 출발` 로 남는다.** 시각은 맞지만
  없어진 정차지를 가리킨다. 실제로 할 일이 아닌 편집이라 두었다.
- 지도 위 식당(#A9700F)과 카페(#9E3B3B)를 색으로만 나눈다(대비 1.59:1, 지름 10px).
  모양을 달리하려면 범례와 세 탭을 함께 손봐야 한다.
- `html{-webkit-text-size-adjust:100%}` 는 그대로 두었다(§8).
